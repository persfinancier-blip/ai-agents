# Промпт: Интеграция 4 несмёрженных веток в main (аккуратно, по зависимостям)
### `chore`-проход · разрешение конфликтов логов «оставить обе стороны» · тесты зелёные после кода

> **Кому:** Claude Code. Перед началом: `git fetch`/`git status` чисто, рабочее дерево без незакоммиченного. Работать локально; push — только по указанию владельца.
> **Контекст:** накопилось 4 ветки поверх `main`; `docs/goal-alignment-model` ответвлён от `docs/reconcile-decisions` (уже содержит его коммиты). Несколько веток трогали `DEVLOG.md`/`BACKLOG.md`/`INDEX.md` — там будут конфликты, разрешаемые сложением содержимого.

## Порядок слияния (по зависимостям)

Слить в `main` строго в этом порядке:

1. **`docs/delegation-policy`** — правит `CLAUDE.md` (+ DEVLOG). Простейшая, первой.
2. **`docs/goal-alignment-model`** — содержит и ADR-0002/§7 (из reconcile-decisions), и ADR-0003/§9. Слить одним махом → в `main` приезжает весь docs-канон. (Ветку `docs/reconcile-decisions` отдельно НЕ сливать — её коммиты уже внутри goal-alignment-model; после слияния проверить `git branch --merged` и удалить reconcile-decisions как влитую.)
3. **`feat/goal-entity-step1-crud`** — код Шага 1 (+ его правки BACKLOG/DEVLOG). Слить последней, т.к. после неё гоняем тесты.

## Правило разрешения конфликтов

- **`DEVLOG.md`** — append-only журнал: при конфликте **сохранить ОБЕ записи**, упорядочив по дате (новые сверху). Ничего не терять.
- **`BACKLOG.md`** — список задач: **объединить обе стороны**, убрать буквальные дубли строк, сохранить отметки `[x]`/`[ ]` актуальными (Шаг 1 — done; Шаги 2a/2b/3 — открыты).
- **`INDEX.md`** — перечень: **объединить**, чтобы присутствовали и ADR-0002, и ADR-0003, и все затронутые файлы.
- **Код и канонические доки** (`Management_Model.md`, ADR-файлы, `*.py`) — конфликтов быть не должно (разные ветки, разные файлы); если возник — остановиться и показать владельцу, не «решать» наугад.

## После каждого слияния
- `git status` чист, коммит-merge осмысленный (`Merge branch '<name>' into main`).
- После слияния ветки — проверить `git branch --merged main` и удалить влитые ветки (`git branch -d <name>`), включая `docs/reconcile-decisions`.

## После слияния кода (шаг 3) — обязательная проверка
```
cd backend && ruff check . && ruff format --check . && python -m mypy app && pytest
cd frontend && npm run build && npm run lint
```
Всё зелёное (27 тестов). Если что-то падает из-за слияния — починить до финала.

## Финал
- В `main` присутствуют: политика делегирования (CLAUDE.md), ADR-0002+§7, ADR-0003+§9, код Goal Шага 1.
- Влитые ветки удалены; остаётся чистый `main` + при необходимости активная рабочая ветка под следующий срез.
- Запись в `DEVLOG.md`: «интеграция веток, main консолидирован».

## Definition of Done
- [ ] 4 ветки слиты в main в порядке 1→2→3; reconcile-decisions удалён как уже-влитый.
- [ ] Конфликты DEVLOG/BACKLOG/INDEX разрешены сложением, ничего не потеряно.
- [ ] backend (ruff/mypy/pytest) и frontend (build/lint) зелёные на main.
- [ ] Влитые ветки удалены; DEVLOG обновлён; push не делался (или сделан по явному указанию).
