# BACKLOG

Задачи и идеи, ещё не взятые в работу. Пока в репозитории нет remote — трекер задач здесь, не в GitHub Issues; при появлении remote можно перенести. Актуальные вехи — таблица в [README.md](README.md).

## Продукт / доки

- [ ] Финальное потребительское имя продукта (кандидат — «Вектор·OS»); зафиксировать в `Visual_Reference.md` D1.
- [ ] Спайк: 2–3 локальные LLM-модели под fallback советников (`docs/adr/0001-local-llm-fallback-for-advisors.md`).
- [x] Открытые вопросы Management_Model §7 — вопросы 1/2/4/7 (формулы увязки, единицы измерения ресурсов, порог «определённости», триггеры пере-увязки) решены под первый срез, см. [`docs/adr/0002-reconciliation-first-slice.md`](docs/adr/0002-reconciliation-first-slice.md).
- [ ] §7 вопрос 3 — права ролей на редактирование чужих узлов; отложено до среза аутентификации/ролей доступа.
- [ ] §7 вопрос 5 — версионирование аналитических справок офисных агентов; отложено до среза офисных агентов (M7–M8).
- [ ] §7 вопрос 6 — каталог навыков (глобальный/по юнитам, переиспользование, Marketplace PRD §39); отложено до среза навыков-воркфлоу.
- [ ] Открытые вопросы бренд-бука Часть II (остаток долга хардкод-цветов после промпта №20, захардкоженные проценты, мёртвые демо-данные `GOAL_CARD`, селф-хостинг шрифтов, эвристика цвета ветки без поля в модели, `ProcessMap.tsx` вне скоупа рестайла — см. «Открытые вопросы» `Visual_Reference.md`). `.ovl.crit` — решено. Визуальный код «бэклога» (пауза на карте) — решено промптом №38b.
- [ ] Миграция других модальных поверхностей на паттерн `Visual_Reference.md` D9 «Оверлеи» (промпт №36 кодифицировал правило только для оверлея разговора и попапа карточки цели): поповеры `GoalCanvas.tsx` (`gc-popover`) и любые будущие оверлеи должны перейти на облачка/затемнение/рамки-идентичности, а не изобретать свою рамку.
- [ ] «Туман-рой» — неоформленные идеи как россыпь тлеющих точек, конденсирующихся в узел карты при оформлении (`Visual_Reference.md` Часть III, «Язык роя точек»); ближнее направление, не спроектировано.
- [ ] Режим «галактика» — смысловая кластеризация больших массивов сущностей (артикулы, знания, находки) роем точек (`Visual_Reference.md` Часть III); дальнее направление, не строить до появления массивов сущностей.
- [ ] Workspace Platform — рабочие пространства сущностей («каждая Сущность имеет пространство»), направление зафиксировано 2026-07-13 в `docs/full-vision/05_Architecture/Workspace_Platform.md`. UI-срез «пространство цели как деталь узла карты» — ПОСЛЕ привязки frontend-карты целей к реальному Goal API (см. «Инженерия»); карта целей остаётся главным экраном.

## Архитектура клиентов (после ядра — не сейчас)

- [ ] Клиентская архитектура: единый React + Web / Desktop (**Tauri**, не Electron без явной нужды в Node-зависимостях) / Mobile (React Native); Desktop несёт локальный AI Runtime (Ollama/LM Studio) — продолжение `docs/adr/0001-local-llm-fallback-for-advisors.md`. Решать ПОСЛЕ того, как ядро (цели/связи/ресурсы) заработает и фронт будет привязан к реальному API — сейчас backend на M3, фронт — прототип на демо-данных, выбор стека преждевременен.
- [ ] (идея, низкий приоритет, скептически) «Enterprise Browser» — рабочее пространство с внешними сервисами (GitHub/Docs/Telegram) внутри приложения. Отдельный крупный продукт, не ядро ОС; оценить целесообразность сильно позже, не закладываться в архитектуру сейчас.
- [ ] Ресурсные блоки (рабочие названия: Финансы / Персонал / Продажи / Товары и услуги / Производство) — предпосылка «факта» у KPI, см. `docs/adr/0005-plan-vs-fact-and-resource-blocks.md`. Отдельная будущая веха; точный набор и границы блоков не решены.

## Инженерия

- [ ] M3 — борд CFO/COO/CTO + синтез рекомендации.
- [x] Goal Entity Шаг 1 — CRUD + автодетекция fog/defined (`compute_definiteness`), см. ветку `feat/goal-entity-step1-crud` (слита в main).
- [x] Goal Entity Шаг 2a — KPI вынесен из JSON-поля `goal.kpis` в отдельную таблицу-сущность (`models/kpi.py`) со своим ID; `compute_definiteness` читает таргеты из неё. См. ветку `feat/goal-step2a-kpi-entity`.
- [x] Goal Entity Шаг 2b — структурное дерево целей: `goal.parent_id` (self-FK), запрет циклов структуры (`would_create_cycle`), поддерево (`get_subtree`). См. ветку `feat/goal-step2b-tree`, слита в main.
- [x] Goal Entity Шаг 3a — граф связей KPI→KPI: таблица `kpi_link` (ребро, не Entity-подтип) с `link_type ∈ {contributes, constrains, depends_on}`; один KPI — источник нескольких связей; циклы разрешены и НЕ детектируются (это Шаг 3c); удаление KPI (точечное и каскадное через удаление цели) чистит его связи в сервисном коде (`kpi_link_service.delete_links_for_kpi`, БД-каскадов в проекте нет). См. ветку `feat/goal-step3a-kpi-links`.
- [x] Goal Entity Шаг 3c — детект циклов графа связей KPI→KPI: таблица `kpi_link_cycle` (персистентные элементарные циклы, пересчёт diff-sync'ом при каждой мутации `kpi_link` — `find_cycles`/`sync_cycles`); циклы НЕ запрещаются (`create_link` не меняет поведения), только фиксируются; ручной флаг `confirmed`, сохраняющийся пока состав цикла не меняется; судья — ближайший общий структурный предок целей-владельцев KPI цикла по `goal.parent_id` (`null`, если предка нет). Соотношение структурной свёртки ресурсов и смысловой увязки (Management_Model §9.8) остаётся отдельным открытым вопросом, эта задача его не закрывает. См. ветку `feat/goal-step3c-cycle-detection`.
- [x] Goal Entity Шаг 3b — составной KPI: таблица `kpi_factor` (вес × KPI-фактор → композитный KPI, не Entity-подтип); значение — сырая взвешенная сумма `Σ weight × factor.target` без нормализации весов (открытый вопрос ADR-0004 намеренно не решён); фактор обязан сам иметь числовой `target` (иначе 400) — это же и структурный запрет рекурсии/циклов в графе факторов, отдельного детекта не требуется; `compute_definiteness` расширен на `measurable` (таргет ИЛИ композитность); каскад удаления KPI (точечный и через удаление цели) чистит и `kpi_link`, и `kpi_factor`. Составные KPI не встраиваются в `POST/PATCH /goals` — отдельный роут `/kpi-factors`, как связи Шага 3a. Модель — `docs/full-vision/02_Product/Management_Model.md` §9.12, `docs/adr/0004-goal-link-types-cycles-composite-kpi.md`. См. ветку `feat/goal-step3b-composite-kpi`.
- [x] Привязка frontend-карты целей к реальному Goal API — Шаг Ф1 (read-only): типы `GoalRead` + клиент в `api.ts`, дерево по `parent_id` (`os/goalTree.ts`), туман для fog, «N KPI» вместо процентов, демо-карта с пометкой при пустой БД, сид `scripts/seed_demo_goals.py`. См. ветку `feat/frontend-goal-map-binding` (слита в main).
- [x] Карточка цели на реальных данных — Шаг Ф2 (read-only): реальные узлы карты кликабельны, новый `RealGoalCard.tsx` (KPI-план, drill-down по `parent_id`/`getGoalSubtree`, состояния загрузка/ошибка/404), демо-путь не задет. См. ветку `feat/frontend-real-goal-card`.
- [x] Редактирование целей из UI — Шаг Ф3 (промпт №22): создание двойным кликом по полотну (черновик-узел без сохранения координат), инлайн-правка в `RealGoalCard.tsx` (имя/описание/владелец, KPI с сохранением id по diff-sync, родитель через поповер с 409-циклом, бэклог-переключатель), удаление с подтверждением и каскадом по 409. Backend и связи KPI (`/kpi-links`, `/kpi-factors`) не тронуты. См. ветку `feat/frontend-goal-editing`.
- [x] Канвас постановки цели — Шаг Ф4 (промпт №23): новый `GoalCanvas.tsx` — радиальная композиция (цель в центре, KPI/подцели по секторам, владелец/чек постановки по бокам), первая визуализация графа увязки KPI→KPI (создать/разорвать связь через поповер, циклы с судьёй и подтверждением, факторы композита read-only пунктиром). Backend не менялся. Цвет типа связи закрывает открытый вопрос ADR-0004 (визуализация связей) для канваса — на общей карте связи по-прежнему не рисуются. См. ветку `feat/frontend-goal-canvas`.
- [x] Управление факторами композитного KPI из UI — Шаг Ф5 (промпт №30): на канвасе постановки клик по собственному KPI открывает выбор действия («связать KPI…» / «добавить фактор…»); добавление фактора мирует флоу связи (цель → KPI с таргетом → вес) и создаёт `POST /kpi-factors`, удаление — по dashed-рёбрам факторов (hit-path как у связей) через `DELETE /kpi-factors/{id}`; ошибки 409/400 — русским текстом. Backend не менялся. Весов PATCH нет (не по скоупу, см. ниже). См. ветку `feat/frontend-kpi-factor-editing`.
- [x] Декомпозиция цели из карточки — Шаг Ф6 (промпт №32): карточка цели разделена на «КОНТЕКСТ» (grandparent + parent, ровно 2 уровня вверх — не вся цепочка до корня, owner-решение 2026-07-15) и «ДЕКОМПОЗИЦИЮ» (прямые подцели + «+ подцель»); «+ подцель» — и в карточке, и на канвасе постановки (узел-черновик dashed в секторе подцелей), обе точки входа создают `POST /goals` с `parent_id` и делают полный `reload()`. Backend не менялся. См. ветку `feat/frontend-goal-decomposition`.
- [x] Карточка цели — попап над картой, страница удалена — Шаг Ф7a (промпт №35, финал): `GoalPopup.tsx` рендерится поверх `CommandPanel`, `RealGoalCard.tsx` удалён; дерево целей теперь рисуется в одном месте (карта). Оверлей — стиль advisor-orb, безрамочный ряд иконок вместо кнопок с подписями. Decomposition-UI из попапа убран — ждёт слайса B. См. ветку `feat/frontend-goal-popup`.
- [ ] Бэкенд-слайс: поля `deadline`/`importance`/`urgency` на цели — сначала ADR + миграция, затем ожить секции «СРОК» и «ВАЖНОСТЬ · СРОЧНОСТЬ» попапа (сейчас dashed-заглушки без данных, owner-решение 2026-07-15: «это задача для бэкенда»).
- [ ] «Назначить процесс» — слайс исполнения цели через процесс, нужен свой ADR; иконка в попапе сейчас disabled-заглушка.
- [x] Юнит-сущность для назначения владельца цели — канон закрыт (ADR-0006, промпт №40): Unit как подтип Entity, `kind ∈ {employee, agent, external, device}`, `goal.unit_id` заменяет текстовый `owner`. Backend-срез — промпт №41 (отдельный проход).
- [x] Backend-срез юнита — промпт №41: таблица `unit` (Entity-подтип), `app/services/unit_service.py` (CRUD, delete нулит `unit_id` на целях), роуты `/api/v1/units`, `goal.unit_id` заменил `entity.owner` в Goal API (`compute_definiteness` теперь читает `unit_id`), одноразовая миграция данных (owner-текст → Unit(kind=employee)) в `alembic/versions/0f9898942202_...py`, сид обновлён. Фронтенд — только read-only отображение `unit_name` (без UI назначения).
- [x] ADR + слайс группировки юнитов (team/dept), backend — промпт №44 (ADR-0007): `unit_group` (Entity-подтип, `kind ∈ {department, team}`), `unit_group.parent_id` (дерево департаментов, один родитель, запрет циклов), `unit.department_id` (один дом на юнит), `team_membership` (m2m-оверлей); `goal.unit_id` перенаправлен с `unit.entity_id` на `entity.id` — цель может принадлежать юниту ИЛИ группе (422 для прочих сущностей); удаление группы нулит входящие ссылки. API `/api/v1/unit-groups` (CRUD + members). Фронтенд: пикер юнит/группа в `GoalPopup` — промпт №45; дерево департаментов + команды в панели юнитов — промпт №46.
- [x] UI-милестон юнитов, слайс 1/3 — список юнитов (промпт №43): read-only панель `UnitsPanel.tsx` (оверлей над картой целей, кнопка «ЮНИТЫ» в хедере `CommandPanel.tsx`), RU-подписи вида юнита (`os/units.ts`), `listUnits()` в `api.ts`. Создание/редактирование/удаление юнитов из UI — не входило в скоуп (read-only срез).
- [x] UI-милестон юнитов, слайс 2/3 — назначение владельца цели из попапа (промпт №45): строка «отв. …» в `GoalPopup.tsx` стала пикером (`OwnerPicker`, D9-порода, `.rpool`-список) с поиском по имени, секциями «Юниты»/«Департаменты»/«Команды» (`listUnits`+`listUnitGroups`), «— не назначен» снимает владельца; `PATCH /goals/{id}` с `unit_id` — юнит или группа (ADR-0007). Backend не менялся. См. ветку `task/prompt-45-goal-owner-picker-20260717`.
- [x] UI-милестон юнитов, слайс 3/3 (финал) — дерево департаментов + команды в панели юнитов (промпт №46): `UnitsPanel.tsx` — рекурсивный лес департаментов по `unit_group.parent_id` с отступом по глубине, юниты под своим `department_id`, секция «Без департамента», секция «Команды» с составом через `listTeamMembers()`. Read-only, backend не менялся. Юниты-UI milestone (список → группировка backend → пикер владельца → дерево) закрыт; редактирование членства — опциональный будущий срез.
- [ ] Увязка юнита как ресурса — соотношение `unit_id` и `OsResource.load` (загрузка юнита в %, Management_Model §3.2), открытый вопрос 5 ADR-0006.
- [x] Назначение родителя из попапа — слайс 37 (промпт №37): строка «родитель: …» в `GoalPopup.tsx` для любой цели (туман/определена), пикер-облачко (`ParentPicker`, D9-порода, `.rpool`-список) со «сделать корневой» первым пунктом, 409-цикл — честная ошибка, без клиентской фильтрации потомков. См. ветку `feat/frontend-popup-parent`.
- [x] Слайс 38 — hover-контролы узла и ребра карты (промпт №38 v2): узел — ряд «+ ‖ ⬡ ×» (подцель через `GoalPopup` в режиме создания / пауза-бэклог / процесс-заглушка / удаление с каскадом); ребро — «+ ×» (вставка цели между P и C двумя мутациями `createGoal`+`patchGoal`, честная ошибка второго шага без отката / разрыв связи через `parent_id: null`). См. ветку `feat/frontend-map-hover-controls`.
- [ ] После ADR процесса — иконка «⬡» на узле карты должна показывать активно/неактивно по признаку назначенного процесса (сейчас всегда inert-заглушка, слайс 38).
- [x] Промпт №38a (фиксы ревью слайса 38): ✕/backdrop в create-режиме `GoalPopup` теперь честная отмена (`skipCreateBlur` на `onMouseDown`), клавиатурный доступ к hover-контролам ребра (`.ehov-wrap`/`.ehov-hotspot` вместо `tabIndex` на SVG `<line>`), `mountedRef`-гвард пережил StrictMode remount, общий `deleteGoalWithCascadeConfirm` вместо дублирования между `GoalPopup`/`CommandPanel`, честная поверхность не-409 ошибок удаления.
- [ ] SVG-глиф дублирование (`HoverGlyph` в `CommandPanel.tsx` vs инлайн-иконки в `GoalPopup.tsx`) и CSS-дублирование `.nhov`/`.ehov`-семейства в `os.css` — не тронуто в промптах №38/38a, свести к общему набору при следующей правке этой зоны.
- [x] Промпт №38b (фикс): паузнутые (`is_backlog`) цели остаются на карте — правило «на карте не размещаются» (`goalTree.ts`) отменено владельцем 2026-07-16; узел паузы приглушён (`.gcard.paused`, сплошная рамка — не fog-код) и помечен «‖ пауза». См. ветку `fix/frontend-paused-on-map`.
- [ ] Hover-контролы карты (узел и ребро) реализованы через React state (`onMouseEnter`/`onMouseLeave`), а не чистый CSS `:hover` — сознательный выбор ради общего состояния с клавиатурным фокусом (см. промпт №38a, п.2), но при будущей ревизии стоит оценить, не упростится ли на CSS `:focus-within`.
- [ ] Мёртвый вариант `{ kind: 'create', parentId: null }` в `GoalPopupMode` (`GoalPopup.tsx`) — на карте create всегда идёт с `parentId` узла; ветка `parentId: null` недостижима из UI слайса 38, оставлена на будущее (создание корневой цели без узла-родителя) или к удалению.
- [x] Слайс 39 — drag рёбер (промпт №39): порт исходящих связей на узле (pointer-down → пунктирное превью D6 → drop на другой узел → `patchGoal(parent_id)`), переподключение через грэб-ринг на родительском конце ребра `.ehov`, честный откат при цикле (409, «Нельзя: цикл в дереве»), Escape/пустой drop — отмена без запросов. Очередь промптов 37–39 завершена. См. ветку `feat/frontend-map-drag-edges`.
- [ ] Типы/подписи рёбер карты (этап-подцель, последовательно/параллельно) — нужен ADR прежде реализации; сознательно не в скоупе слайса 39 (см. промпт №39, «Не делать»).
- [ ] CSS: прибраться в осиротевших `.os-goal`-правилах `os.css` — после удаления `RealGoalCard.tsx` (Ф7a) селекторы страницы (`.hd`, `.chain`, `.cols`, `.wstage` и т.п., не переиспользованные попапом) больше никем не используются; сознательно не тронуты в промпте №35 (не по скоупу).
- [ ] Бизнес-процесс исполнения цели (не постановки) — требует пару ADR + backend-срез; owner-решение 2026-07-15, отдельная будущая веха.
- [ ] Раскладка карты целей: перекрытие узлов на плотных картах — отдельный срез по layout (сейчас простая тригонометрия по секторам, без force-directed); owner-решение 2026-07-15.
- [ ] CI: проверить пути `.github/workflows/ci.yml` после переезда `decision-center/` в корень (working-directory `backend`/`frontend` уже относительные — но прогнать реальный workflow до первого push).
- [ ] GitHub MCP-сервер — добавить в `.mcp.json`, когда появится remote (см. README «MCP»).
- [x] Шаг 3a — diff-sync KPI в `patch_goal` (`sync_kpis`, сопоставление по id вместо
  replace-all): было предусловием Шага 3 (связи KPI→KPI не переживут replace-all,
  меняющий `entity_id` при каждом патче цели). См. ветку `feat/goal-step3-0-kpi-diff-sync`,
  слита в main.
- [ ] `goal_service.list_goals` тянет KPI отдельным запросом на каждую цель (N+1).
  Оптимизировать (bulk-загрузка KPI по списку goal_id) при росте числа целей.
- [ ] Редактирование веса существующего фактора композита (PATCH `/kpi-factors/{id}`) — сейчас только удалить+добавить заново (Шаг Ф5, промпт №30); отдельный будущий срез, если удалить+добавить окажется неудобным на практике.
- [x] Доработать `.claude/hooks/protect-main.sh`: (а) ловит только `Edit|Write|NotebookEdit`
  — запись через Bash его обходит (дыра); (б) блокирует легитимное разрешение
  merge-конфликтов на `main`. Закрыто промптом №25 (`chore/config-hygiene`, PR #3):
  добавлен матчер `Bash`, исключение — активный `MERGE_HEAD`.
- [x] Рестайл frontend под дизайн v2 (промпт №20): токены, стеклянные узлы, плавающие
  пилюли, компонент `AdvisorOrb` (canvas), вертикаль орбов + оверлей разговора с темами;
  логика/API не трогали. См. `Visual_Reference.md` Часть III (канон). Ветка `feat/frontend-restyle-v2`.
