# DEVLOG

Журнал проходов по репозиторию: дата · ветка/коммит · что сделано · что дальше. Новые записи — сверху. Добавляется командой `/devlog`.

## 2026-07-13 · feat/frontend-real-goal-card · не закоммичено

- **Что сделано:**
  - Реальные узлы карты (`RealGoalMap` в `CommandPanel.tsx`) стали кликабельными: клик вызывает `onOpenGoal(id, 'real')`, вместо демо-заглушки-тултипа.
  - Новый компонент `frontend/src/os/RealGoalCard.tsx` — карточка цели на реальных данных (`GoalRead`), независимый от демо-`GoalCard.tsx`: топ-бар (имя, чип lifecycle `.chip.lc-*`, бейдж «туман»/«определена», «назад к карте»), шапка `.hd` (описание, владелец, `role_label`), KPI-плитки `.kpis > .k` с подписью «ПЛАН» (композитные — с пометкой «расчёт», без таргета — «—»), навигация по структуре (строка родителя через `getGoal`, прямые дети через `getGoalSubtree`, drill-down/up по `goalPath` из `App.tsx`).
  - `App.tsx` разводит источник открытой карточки (`goalSource: 'demo' | 'real'`) — демо-карта по-прежнему открывает демо-`GoalCard`, реальная — `RealGoalCard`; демо-путь на пустой БД не задет.
  - `api.ts`: добавлен класс `ApiError` (несёт HTTP-статус) — нужен, чтобы отличать 404 (цель удалили) от прочих отказов сети/бэка.
  - Ручная проверка в браузере (Playwright): демо-путь на пустой БД не сломан; сид `scripts/seed_demo_goals.py` → клик по реальному узлу открывает карточку с KPI-планами; drill-down к прямой подцели и обратно к родителю; «назад к карте»; 404 при удалённой цели показывает сообщение и не роняет навигацию. Backend контрольный прогон (`pytest` 103, `ruff`, `ruff format --check`, `mypy`) и frontend (`build`, `lint`) — зелёные.
- **Дальше:**
  - Следующий срез — редактирование карточки цели (create/patch/delete из UI), см. `prompts/prompt-18-real-goal-card.md` «НЕ делаем».
  - Визуализация связей KPI→KPI и циклов (`/kpi-links`) в UI — открытый вопрос ADR-0004, отдельный срез.
  - Советник по целям в реальной карточке — отдельная веха (M-советники), не в этом срезе.

## 2026-07-13 · feat/frontend-goal-map-binding

- **Что сделано (Шаг Ф1, промпт №17):**
  - TS-типы `GoalKpiRead`/`GoalRead` в `types.ts` — зеркало pydantic-схем `backend/app/schemas/goal.py`; клиент целей в `api.ts` (`listGoals`/`getGoal`/`getGoalSubtree`) тем же `request<T>`-хелпером, что и Decisions.
  - Новый модуль `os/goalTree.ts` (не смешан с демо-хелперами `goals.ts`): `buildGoalForest` — лес по `parent_id`, сироты поднимаются в корни, `is_backlog` на карту не попадает; `layoutForest` — простая автораскладка «глубина = колонка, порядок обхода = строка» с шагами из габаритов демо-узлов (w 215, колонка 250, строка 118). Сознательно без force-directed — временная раскладка до отдельного среза UI карты.
  - `CommandPanel` читает реальные цели через `useEffect` (по образцу `listDecisions`): состояния загрузка / ошибка («Не удалось загрузить карту целей») / пусто / данные. Реальный узел `.gcard`: бейдж «туман»/«определена», тон по `risk_level` (low → обычный, medium → warn, high → risk), туман — существующий пунктирный код «нет данных» (`.g-next`, бренд-бук D6, новых стилей нет), в футере «N KPI» или «туман» + «отв. …». **Прогресс-процентов нет сознательно** — факта KPI не существует до ресурсных блоков (ADR-0005), демо-`pct` на реальные цели не переносится. Рёбра — только структурное дерево `parent_id` (визуализация связей/циклов KPI — открытый вопрос ADR-0004, не здесь). Фокус-блок `.gx` — первая корневая цель: «этапы» — прямые дети (имя + владелец + определённость), футер res/team/dec и «карточка цели →» скрыты (этих данных на бэке нет). Пустая БД → прежняя демо-карта целиком с пометкой «ДЕМО-ДАННЫЕ» в подписи карты; демо-константы `data.ts` не тронуты, `GoalCard` остаётся на `GOAL_TREE` (отдельный срез). Реальные узлы пока некликабельны (карточка цели на реальных данных — следующий срез).
  - Сид `backend/scripts/seed_demo_goals.py` (`python -m scripts.seed_demo_goals` из `backend/`): 6 целей через `goal_service.create_goal` (не сырым SQL) — корень «Выручка 120 млн ₽ / год» с 2 KPI-таргетами, 4 ребёнка (двое в тумане: «Пилоты в enterprise» без owner и KPI, «Партнёрский канал» с KPI без таргета), отдельный корень «Найм 6 инженеров». Идемпотентность: при непустой БД выходит, ничего не создавая. Dev-утилита, из приложения не импортируется, тестов на неё нет. `scripts/` добавлен в `files` mypy (`pyproject.toml`, без ослабления правил); ruff покрывает его и так (`ruff check .`); канонический вызов `mypy app` скрипты не видит — для них `python -m mypy` без аргументов.
  - Проверки в сессии: `tsc -b` — зелёный; `py_compile` сид-скрипта — ок; `pyproject.toml` парсится. Полный набор (pytest/ruff/ruff format/mypy, `vite build`, `oxlint`) в песочнице Cowork недоступен — нет выхода к pypi/npm, нативные бинари в `node_modules` установлены под Windows (подробности в записи прохода №16). Владельцу прогнать локально: `cd backend` → четыре проверки; `cd frontend` → `npm run build`, `npm run lint`; затем ручная проверка — поднять backend, прогнать сид, `npm run dev` (карта с туманом; при пустой БД — демо с пометкой).
- **Дальше:**
  - Следующие срезы фронта: карточка цели на реальных данных (клик по реальному узлу), редактирование целей из UI, визуальный код «бэклога» (открытый вопрос бренд-бука Часть II).
  - Открытые вопросы ADR-0004 (визуализация связей/циклов на карте) и ADR-0005 (факт KPI из ресурсных блоков) этим проходом не решались.

## 2026-07-13 · chore/repo-integrity-recovery

- **Что сделано (инцидент-проход, промпт №16):**
  - **Бэкап до любых действий:** `git bundle --all` со всей историей (verify: complete history), сохранён вне рабочей папки. Push в origin не выполнен: remote в `.git/config` есть, но в сессии Cowork нет учётных данных — владельцу пушнуть вручную.
  - **Диагностика «массовой обрезки»:** все 18 «модифицированных» файлов прочитаны с диска напрямую, построчно. Итог: 17 — категория (а), ложные модификации (контент байт-в-байт равен HEAD; «обрезку» показывал только слой синхронизации песочницы Cowork: kpi_link_service.py — 98 строк из 265, config — 7 строк из 16); 1 — категория (в), `prompts/README.md` — легитимная правка владельца (строки промптов №16/№17 в таблице «Активные»). Категории (б) — реального повреждения — не нашлось ни в одном файле.
  - **Реальные дефекты оказались в служебных файлах git, не в контенте:** (1) `improper chunk offset` — битый **multi-pack-index** (commit-graph ни при чём: файла нет, `commit-graph verify` чист); (2) битая контрольная сумма индекса (`git fsck`: bad index file sha1 signature — обычные команды не проверяют её, потому `status` работал); (3) протухший 5-часовой `index.lock` от упавшего процесса — он же держал аномалию `prompts/_done/` (все 20 файлов на диске md5-идентичны HEAD, записи просто выпали из индекса).
  - **Лечение:** удалён протухший lock → индекс пересобран из HEAD (`git read-tree HEAD`, рабочие файлы не тронуты; записи `prompts/_done/` вернулись) → `git multi-pack-index write` (verify чист) → 17 файлов категории (а) переписаны `git checkout --` (безопасно: равенство HEAD доказано прямым чтением) — это же обновило протухший кэш песочницы. `git fsck --full` чист (только безобидные dangling), chunk-ошибка ушла.
  - **Верификация:** песочница Cowork не имеет выхода к pypi/npm (реестры закрыты сетевой политикой) и не может исполнять Windows-бинарники — полный прогон pytest/ruff/mypy/vite/oxlint в сессии невозможен. Доказательство эквивалентно: backend/frontend-контент байт-идентичен HEAD `b3207c2`, чьи проверки зелёные (103 passed, DEVLOG 8c12939; 54b8daf трогал только доки); `tsc -b` прогнан в песочнице — зелёный. Владельцу: прогнать четыре backend-проверки + `npm run build && npm run lint` локально для контрольного подтверждения.
  - Хвосты прошлого прохода: заголовок записи workspace-platform-canon уточнён (54b8daf, слита в main b3207c2), выполненный пункт про перенос промпта №15 убран из «Дальше»; `prompt-14-step3b-composite-kpi.md` перенесён в `prompts/_done/`, таблица «Активные» в `prompts/README.md` обновлена.
- **Рекомендации владельцу:**
  - Проверить, что лежит над `C:\Dev` (облачная синхронизация, антивирус с «отложенным» сканированием): слой отдаёт файлы транзиентно недописанными и с протухшим stat — источник и «обрезки», и, вероятно, битого индекса.
  - Перед запуском следующего прохода из Cowork сверять `git status` с прямым чтением файлов; «массовые модификации» без правок — признак протухшего кэша, не повреждения.
- **Дальше:** промпт №17 — карта целей `CommandPanel` на реальном Goal API (read-only).

## 2026-07-13 · docs/workspace-platform-canon · 54b8daf (слита в main, b3207c2)

- **Что сделано:**
  - Зафиксирован новый канон `docs/full-vision/05_Architecture/Workspace_Platform.md` (Architecture Direction, не активный спек) — направление «Every Entity Has Workspace»: рабочее пространство сущности как универсальный UI-слой над Entity Platform, собираемый из возможностей Business Capability Map.
  - Явно закреплено позиционирование: карта целей остаётся главным экраном (`Visual_Reference.md` §2 по сути не меняется), пространство — «внутренность» узла при зуме, не замена карты; вариант «Workspace OS вместо карты» отклонён владельцем.
  - Блоки-районы Command Center, Workspace конструктора дашбордов (`Dashboard_Builder.md`) и Decision Canvas зафиксированы как частные случаи одного механизма пространств.
  - Точечные правки-ссылки: `INDEX.md` (строка про новый файл), `Entity_Platform.md` (связанные документы + §20), `Visual_Reference.md` (§3 зум в узел, §6 блоки — частный случай), `Dashboard_Builder.md` (§4 уточнение терминологии), `BACKLOG.md` (пункт «Продукт/доки»).
  - Проверено перед коммитом: `git fsck` чист (только безобидные dangling-объекты), все кросс-ссылки резолвятся на существующие файлы, типы Entity `Workspace`/`Widget` уже есть в классификации `Entity_Platform.md` §6 (новых моделей данных не появилось), диффы точечные — без побочных правок вне списка Scope.
  - PRD и `Management_Model.md` сознательно не тронуты — принцип пока направление, не требование владельца.
- **Дальше:**
  - Реализация первого пространства (деталь узла карты целей) — не раньше, чем карта целей будет привязана к реальному Goal API (см. BACKLOG, «Инженерия»).

## 2026-07-13 · main · 8c12939

- **Что сделано:** ветка `feat/goal-step3b-composite-kpi` (составной KPI, `kpi_factor`) слита в `main` без конфликтов (`git merge --no-ff`); после слияния перепроверены все четыре проверки на `main` — `pytest` (103 passed), `ruff check`, `ruff format --check`, `mypy app` — зелёные. Ветка удалена (правило: не копить ветки).
- **Дальше:** Шаги 3a/3b/3c завершают модель графа целей на уровне backend. Открытые вопросы ADR-0004 (нормализация весов, визуализация типов связей/циклов на карте) и Management_Model §9.8 (структурная свёртка ресурсов vs смысловая увязка) остаются нерешёнными — дальше либо ресурсы, либо фронт.

## 2026-07-13 · feat/goal-step3b-composite-kpi

- **Что сделано:**
  - Составной KPI (Шаг 3b): таблица `kpi_factor` — вес × KPI-фактор → композитный KPI (не Entity-подтип, тот же аргумент, что `kpi_link`/`kpi_link_cycle`: это связь между уже существующими KPI, не новый узел графа).
  - `computed_value` = сырая `Σ weight × factor.target`, **без нормализации весов** (ADR-0004 «Открытые вопросы» п.2 явно откладывает это) — считается на чтении (`kpi_factor_service.compute_value`), не хранится и не кэшируется.
  - Заслон от рекурсии: фактор обязан сам иметь числовой `target` (`FactorNotMeasurableError` иначе) — композитный KPI (у которого `target`, как правило, `None`) физически не может стать чужим фактором, поэтому в графе факторов структурно не может быть циклов, отдельный алгоритм детекта (как в Шаге 3c для связей) не нужен.
  - `goal_service.compute_definiteness` переписан на сигнатуру `(owner, kpi_measurable: list[bool])` — расширяет «измерим» с «есть числовой target» на «target ИЛИ композитность»; вычисление флага на KPI перенесено в вызывающий код (`to_goal_read`), который стал `async` и делает bulk-запрос `kpi_factor_service.composite_kpi_ids` перед формированием списка (без N+1). Обновлены все 4 прямых юнит-теста на новую сигнатуру.
  - `GoalKpi` получил `computed_value: float | None = None` — заполняется только на чтении для композитных KPI, игнорируется на create/patch (как уже игнорируется `id`).
  - Составные KPI НЕ встраиваются в `kpis` пейлоада `POST/PATCH /goals` — управляются отдельным `/kpi-factors` (курица-яйцо: фактор должен уже существовать своим `entity_id`, а `kpis` цели создаются в том же запросе).
  - Каскад удаления KPI (`kpi_service.delete_kpi`/`delete_kpis_for_goal`) теперь чистит и `kpi_link`, и `kpi_factor` — `kpi_factor_service.delete_factors_for_kpi` удаляет строки, где KPI фигурирует как композит ИЛИ как фактор (симметрично Шагу 3a).
  - Миграция `add kpi_factor table` — чистый `create_table`, ни JSON-колонок, ни ALTER (в отличие от Шагов 2b/3c), никаких quirks; upgrade/downgrade прогнаны туда-обратно.
  - `kpi_factor` зарегистрирован в импортах моделей (`alembic/env.py`, оба тестовых регистратора).
  - Тесты: юнит (`test_kpi_factor_service.py` — self-factor/несуществующий KPI/фактор без target/дубль по отдельности, `compute_value` — сумма и `None` без факторов, композитный KPI без target, но с фактором → measurable) + интеграционные (`test_kpi_factors.py` — канонический пример гудвилла со взвешенной суммой и `definiteness=defined`, композит как единственный KPI цели тоже `defined`, 400 на factor без target/self-factor/несуществующий KPI, 409 на дубль, удаление фактора и удаление фактора-KPI через diff-sync-патч цели, удаление составного KPI чистит его `kpi_factor`-строки). `pytest` (103), `ruff`, `ruff format`, `mypy` — зелёные.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки).
  - Открытые вопросы ADR-0004 (нормализация весов, визуализация типов связей/циклов на карте) и Management_Model §9.8 (структурная свёртка ресурсов vs смысловая увязка) остаются нерешёнными — Шаги 3a/3b/3c завершают модель графа целей на уровне backend, дальше — либо ресурсы, либо фронт.

## 2026-07-13 · main · 72c8d2f

- **Что сделано:** ветка `feat/goal-step3c-cycle-detection` (детект циклов графа связей KPI→KPI, `kpi_link_cycle`) слита в `main` без конфликтов (`git merge --no-ff`); после слияния перепроверены все четыре проверки на `main` — `pytest` (86 passed), `ruff check`, `ruff format --check`, `mypy app` — зелёные. Ветка удалена (правило: не копить ветки).
- **Дальше:** Шаг 3b — составной KPI. Открытые вопросы ADR-0004 (визуализация типов связей/циклов на карте) и Management_Model §9.8 (структурная свёртка ресурсов vs смысловая увязка) остаются нерешёнными.

## 2026-07-13 · feat/goal-step3c-cycle-detection

- **Что сделано:**
  - Детект циклов графа связей KPI→KPI (Шаг 3c): таблица `kpi_link_cycle` — персистентные элементарные циклы, каждый со своей строкой (`member_kpi_ids`, `member_link_ids`, ручной флаг `confirmed`, `judge_goal_id`). Циклы по-прежнему НЕ запрещаются — `create_link` не блокирует замыкающую связь, только фиксирует её в таблице циклов.
  - `find_cycles(links) -> list[Cycle]` — чистая функция: DFS от каждого узла по текущему простому пути, замыкание на стартовый узел даёт цикл; нормализация ротацией к лексикографически наименьшему id узла даёт устойчивый `canonical_key`, дедуп по нему схлопывает переоткрытия одного и того же цикла с разных стартовых узлов. Покрыта юнит-тестами отдельно от сессии БД (`test_kpi_link_cycles_service.py`).
  - `sync_cycles(session)` — пересчитывает все циклы целиком при каждой мутации графа (дёшево при небольшом графе, тот же аргумент, что ADR-0002 §7.7) и сопоставляет с текущими строками `kpi_link_cycle` по `canonical_key`: совпадающие обновляются на месте (флаг `confirmed` сохраняется), исчезнувшие удаляются, новые создаются с `confirmed=False`. Вызывается из всех трёх точек мутации графа: `create_link`, `delete_link`, `delete_links_for_kpi` (последняя — уже существующая точка каскада Шага 3a, значит удаление KPI точечно или через каскад цели тоже подчищает циклы).
  - Судья цикла — ближайший общий структурный предок (`goal.parent_id`) целей-владельцев KPI цикла; локальная функция обхода предков (`_goal_ancestor_chain`) вместо импорта `goal_service`, чтобы не создавать цикл импортов (`goal_service` → `kpi_service` → `kpi_link_service`); при отсутствии общего предка — `judge_goal_id = null`, это легитимное состояние, не ошибка.
  - Докстринг `create_link` обновлён — раньше говорил, что циклы никак не отслеживаются; теперь отражает, что связь по-прежнему не блокируется, но теперь запускает пересчёт `kpi_link_cycle`.
  - Роуты `GET /kpi-links/cycles` и `PATCH /kpi-links/cycles/{id}` — зарегистрированы ДО `GET/DELETE /kpi-links/{link_id}`, чтобы FastAPI не спутал `cycles` со значением `{link_id}`. Подтверждение — без проверки прав (та же логика, что `is_backlog`: семантика ответственности для человека, не access control).
  - Миграция `add kpi_link_cycle table` — `create_table`, известный автоген-quirk (`Text`-импорт для JSONB-варианта) поправлен; upgrade/downgrade прогнаны туда-обратно.
  - `kpi_link_cycle` зарегистрирован в импортах моделей (`alembic/env.py`, оба тестовых регистратора).
  - Тесты: интеграционные (`test_kpi_link_cycles.py` — цикл из 2 и 3 узлов появляется/исчезает, `confirmed` переживает несвязанное изменение графа, разрыв+пересоздание цикла сбрасывает `confirmed` (новая строка — новая идентичность), судья на дереве с общим предком и на несвязанных ветках (`null`), 404 на подтверждение несуществующего цикла, каскад через diff-sync-патч цели убирает и связь, и цикл) + юнит (`find_cycles` — пустой граф, без циклов, один/два цикла, нормализация не зависит от стартовой точки, отсутствие ложных срабатываний на общем узле без цикла). `pytest` (86), `ruff`, `ruff format`, `mypy` — зелёные.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки).
  - Открытые вопросы ADR-0004 (визуализация типов связей и циклов на карте — фронт, отдельный срез) и соотношение структурной свёртки ресурсов и смысловой увязки (Management_Model §9.8) — не решены этой задачей, остаются открытыми.
  - Шаг 3b — составной KPI.

## 2026-07-13 · feat/goal-step3a-kpi-links

- **Что сделано:**
  - Граф связей KPI→KPI (Шаг 3a): таблица `kpi_link` — направленное ребро (source, target, link_type), НЕ Entity-подтип (узлы графа — KPI-сущности, ребру полноценная Entity избыточна). Три типа: `contributes`/`constrains`/`depends_on` (ADR-0004 Р1).
  - `kpi_link_service`: `create_link` (валидация — оба KPI существуют, `source != target`, дубль `(source, target, type)` → 409), `get_link`/`list_links`/`list_links_for_kpi` (источник ИЛИ получатель), `delete_link`.
  - Циклы графа связей НЕ проверяются — связь, замыкающая цикл (A→B и B→A), создаётся штатно; детект — отдельный Шаг 3c.
  - Р2 (связь умирает с концом): каскад — в сервисном коде, не в БД (в проекте нет `ON DELETE CASCADE` нигде — Goal/Kpi тоже чистятся вручную). `kpi_link_service.delete_links_for_kpi` вызывается из `kpi_service.delete_kpi` (точечное удаление) и `kpi_service.delete_kpis_for_goal` (каскад цели) — после удаления KPI (точечно через diff-sync-патч или каскадно через удаление цели) не остаётся висячих строк `kpi_link`.
  - Роут `app/api/v1/kpi_links.py`: `POST/GET/GET{id}/DELETE /kpi-links`, `?kpi_id=` для фильтра; роутер подключён.
  - Миграция `add kpi_link table` — `create_table`, без ALTER существующих таблиц, поэтому SQLite batch-mode не потребовался (в отличие от Шага 2b); upgrade/downgrade прогнаны туда-обратно.
  - `kpi_link` добавлен в импорты моделей (`alembic/env.py`, оба тестовых регистратора).
  - Тесты: интеграционные (`test_kpi_links.py` — три типа, 422 на невалидный тип, 400 несуществующий KPI/self-link, 409 дубль, множественность источника, `?kpi_id=` по обеим ролям, цикл создаётся штатно, delete 204/404, Р2-каскад через diff-sync-патч цели и через каскадное удаление цели) + юнит (`test_kpi_link_service.py` — валидации, `delete_links_for_kpi` чистит обе стороны). `pytest` (70), `ruff`, `ruff format`, `mypy` — зелёные.
  - Замечен и поправлен (уже зафиксировано в BACKLOG параллельной доковой веткой `docs/plan-fact-and-clients`, продублировано здесь для целостности этой ветки): устаревшая отметка Шага 3a diff-sync помечена выполненной.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки); учесть возможный конфликт в `BACKLOG.md` с параллельной доковой веткой `docs/plan-fact-and-clients` (обе трогают одну и ту же строку про diff-sync) — разрешить в пользу более полной формулировки.
  - Шаг 3c — детект циклов графа связей (флаг + подсветка, судья = ближайший общий структурный предок).
  - Шаг 3b — составной KPI (после 3c).

## 2026-07-08 · docs/plan-fact-and-clients

- **Что сделано:** зафиксированы в каноне два будущих направления, обсуждённых параллельно коду Шага 3a — не реализовывать сейчас. (1) **План/факт:** KPI сейчас = только план (`target`); зафиксирован принцип «факт — второе измерение рядом с планом, тянется из ресурсного блока (Финансы/Персонал/Продажи/Товары и услуги/Производство — рабочие названия), а не вводится вручную» с примерами источников (прибыль/выручка → Финансы; укомплектованность → Персонал; брак → Производство; источник «отзывов» не определён; «удалённость от метро» — редкий случай ручного ввода). Ресурсных блоков ещё нет — это предпосылка факта, отдельная будущая веха; граф связей и Шаги 1–3b не меняются. Добавлен `Management_Model.md` §9.13 (короткая врезка, ссылка на ADR). Создан `docs/adr/0005-plan-vs-fact-and-resource-blocks.md` (**Proposed** — направление, не решение к реализации) с 4 открытыми вопросами (границы блоков, модель источника факта, частота обновления, источник «отзывов»). (2) **Клиентская архитектура:** зафиксировано направление (единый React, Web/Desktop-Tauri/Mobile-React Native, локальный AI Runtime на Desktop как продолжение ADR-0001) и явно отмечено, что выбор стека сейчас преждевременен (backend на M3, фронт на демо-данных) — решать после привязки фронта к реальному API; «Enterprise Browser» зафиксирован как идея низкого приоритета, не ядро ОС. Заодно поймал и поправил две устаревшие отметки в `BACKLOG.md`: diff-sync KPI в `patch_goal` (Шаг 3a) уже слит в main веткой `feat/goal-step3-0-kpi-diff-sync`, но пункт всё ещё значился невыполненным. Синхронизированы `docs/adr/README.md`, `docs/full-vision/INDEX.md`.
- **Дальше:** Шаг 3a (граф связей KPI→KPI) продолжается параллельно на своей ветке — эта ветка трогает только доки, конфликтов не создаёт. Ресурсные блоки и клиентская архитектура — не брать в работу до готовности их предпосылок (ядро целей/связей/ресурсов и привязанный к API фронт соответственно).

## 2026-07-08 · main · a190759

- **Что сделано:** ветка `feat/goal-step3-0-kpi-diff-sync` (diff-sync KPI вместо replace-all) слита в `main` без конфликтов (`git merge --no-ff`); после слияния перепроверены все четыре проверки на `main` — `pytest` (53 passed), `ruff check`, `ruff format --check`, `mypy app` — зелёные. Ветка удалена (правило: не копить ветки).
- **Дальше:** Шаг 3a — связи KPI→KPI на базе стабильных id.

## 2026-07-08 · feat/goal-step3-0-kpi-diff-sync · 4e9dc5b

- **Что сделано:**
  - Заменён replace-all на diff-sync в `patch_goal`: неизменные KPI сохраняют свой `entity_id` при патче цели (ADR-0004 Р2 — предусловие для связей KPI→KPI в Шаге 3a, чтобы будущие связи не рвались молча при каждом патче).
  - `GoalKpi` получил опциональное `id: str | None = None`: на read — заполняется `entity_id` KPI, на patch — ключ сопоставления (с `id` → update in place; без `id` → create; текущий id, отсутствующий во входящем списке → delete). Форма API обратно совместима — create по-прежнему принимает `kpis` без id.
  - `goal_service.sync_kpis(session, goal_id, incoming)`: валидирует, что все входящие id принадлежат цели (иначе `GoalKpiNotFoundError` → 400), затем удаляет отсутствующие, обновляет существующие, создаёт новые.
  - `kpi_service` получил точечные `get_kpi`/`update_kpi`/`delete_kpi` — не трогают остальные KPI цели; `delete_kpis_for_goal` (bulk) сохранён для каскадного удаления цели целиком.
  - Миграция не потребовалась — схема БД не менялась (id KPI это уже существующий `entity_id`); подтверждено `alembic check` (`No new upgrade operations detected`).
  - Тесты: ключевой — id KPI не меняется при патче с обновлённым target; + добавление нового KPI рядом с существующими, удаление опущенного (без сиротской entity-строки), 400 на несуществующий id. Интеграционные и юнит-тесты (`pytest`, 53), `ruff`, `ruff format`, `mypy` — зелёные.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки).
  - Шаг 3a — связи KPI→KPI (граф увязки) на базе стабильных id, сохранённых diff-sync.

## 2026-07-08 · docs/goal-links-model

- **Что сделано:** зафиксирована в каноне модель связей целей, обсуждённая для Шага 3 — закрывает все 4 открытых вопроса ADR-0003: (Р1) связи типизированы (`contributes`/`constrains`/`depends_on`), одна KPI-сущность может участвовать в нескольких связях разных типов одновременно; (Р2) KPI — источник истины связи, удаление любого её конца каскадно удаляет саму связь (без осиротевших ссылок), что делает diff-sync в `patch_goal` предусловием Шага 3, а не отдельной оптимизацией; (Р3) связи разнонаправленные (правила «только вверх» нет), циклы графа связей не запрещаются, а детектируются и подсвечиваются, разрешить/разорвать цикл может только ближайший общий структурный предок узлов цикла по `parent_id` — граф связей ставит вопрос, дерево структуры назначает ответственного; (Р4) качественные KPI без числа раскладываются на составной KPI — взвешенную сумму факторов-KPI (точечный инструмент, отдельный Шаг 3b), правило тумана расширено (таргет ИЛИ формула). `Management_Model.md` §9 расширен подразделами 9.9–9.12, диаграмма §9.7 дополнена типами рёбер и множественностью `[K4]`, добавлена мини-диаграмма цикла+судьи в §9.11, уточнён пункт «две оси» в §9.8. Создан `docs/adr/0004-goal-link-types-cycles-composite-kpi.md` (Accepted, модель) с таблицей закрытия вопросов ADR-0003 и 2 открытыми вопросами (визуализация типов связей на карте, нормализация весов факторов). Синхронизированы `docs/adr/README.md`, `docs/full-vision/INDEX.md`, `BACKLOG.md` (Шаг 3 детализирован, добавлен Шаг 3b; заодно отмечен как выполненный Шаг 2b — был помечен `[ ]` в BACKLOG, хотя уже слит в main веткой `feat/goal-step2b-tree`).
- **Дальше:** промпт реализации Шага 3 (граф связей + diff-sync-предусловие) — следующий кодовый срез backend; Шаг 3b (составной KPI) — после него. Открытые вопросы ADR-0004 (визуализация, нормализация весов) не решать без отдельного обсуждения.

## 2026-07-08 · main · 924675a

- **Что сделано:** ветка `feat/goal-step2b-tree` (структурное дерево целей: `parent_id`, запрет циклов, поддерево, каскадное удаление) слита в `main` без конфликтов (`git merge --no-ff`); после слияния перепроверены все четыре проверки на `main` — `pytest` (46 passed), `ruff check`, `ruff format --check`, `mypy app` — зелёные. Ветка удалена (правило: не копить ветки).
- **Дальше:** Шаг 3 — граф связей KPI→KPI + ресурсы узла.

## 2026-07-08 · feat/goal-step2b-tree · 1cfdbab

- **Что сделано:**
  - Goal Entity, Шаг 2b — структурное дерево: `goal.parent_id` (self-FK на `entity.id`, nullable, индекс). Явно отделено от смысловых связей KPI→KPI (Шаг 3) — в докстринге и коде нет слова «декомпозиция».
  - Запрет циклов: `would_create_cycle(session, goal_id, new_parent_id)` — чистая функция, поднимается по цепочке предков от кандидата-родителя; покрыта unit-тестами (прямой цикл, транзитивный, валидный перенос).
  - `get_subtree(session, goal_id)` — плоский список поддерева (BFS по `parent_id`, обход в Python, включая сам узел).
  - `delete_goal(session, goal_id, cascade)` → `"ok" | "has_children" | "not_found"`: без `cascade` и с детьми — блок; с `cascade=True` — удаление всего поддерева от листьев к корню, включая KPI каждого узла (`kpi_service.delete_kpis_for_goal`), без сиротских entity/kpi строк.
  - Роуты: `GET /goals/{id}/subtree` (плоский список), `DELETE /goals/{id}?cascade=` (404/409/204); create/patch принимают `parent_id`, при несуществующем родителе — 400, при цикле — 409.
  - Определённость НЕ агрегируется по детям — `compute_definiteness` не тронут, родитель в тумане/определён только по своим KPI.
  - Миграция `add goal.parent_id`: автоген сгенерировал `create_foreign_key`/`drop_constraint` вне batch-режима, что падает на SQLite (`NotImplementedError: No support for ALTER of constraints`) — переписано через `op.batch_alter_table` (известный SQLite-quirk, добавлен в миграцию явным комментарием); upgrade/downgrade прогнаны туда-обратно на чистой БД.
  - Тесты: интеграционные (parent_id на create/read, цикл прямой/транзитивный, несуществующий parent_id не 500, subtree на дереве из 3 уровней и для среднего узла, каскад 409/204, определённость не меняется от детей) + юнит (`would_create_cycle` все ветки, каскад чистит KPI-сущности). `pytest` (46), `ruff`, `ruff format`, `mypy` — зелёные.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки).
  - Шаг 3 — граф связей KPI→KPI + ресурсы узла; при diff-sync KPI (см. BACKLOG) учитывать, что `entity_id` KPI теперь должен переживать патчи цели, чтобы связи не рвались.

## 2026-07-08 · docs/backlog-pre-2b · не закоммичено

- **Что сделано:** в `BACKLOG.md` (раздел «Инженерия») добавлены три пункта перед Шагом 2b: (1) заменить replace-all в `patch_goal` на diff-sync KPI до появления связей KPI→KPI в Шаге 3 (целостность `entity_id`); (2) N+1-запрос KPI в `goal_service.list_goals` — оптимизировать при росте числа целей; (3) доработать `.claude/hooks/protect-main.sh` — не ловит запись через Bash, блокирует легитимные merge-конфликты на `main`. Код не тронут.
- **Дальше:** Шаг 2b — структурное дерево целей (`parent_id`).

## 2026-07-08 · main · a9ecce0

- **Что сделано:** ветка `feat/goal-step2a-kpi-entity` (KPI как отдельная Entity-сущность) слита в `main` без конфликтов (`git merge --no-ff`); после слияния перепроверены все четыре проверки на `main` — `pytest` (30 passed), `ruff check`, `ruff format --check`, `mypy app` — зелёные. Ветка удалена (правило: не копить ветки).
- **Дальше:** Шаг 2b — структурное дерево целей (`parent_id`); Шаг 3 — граф связей KPI→KPI + ресурсы узла.

## 2026-07-08 · feat/goal-step2a-kpi-entity · 91958fe

- **Что сделано:**
  - KPI вынесен из JSON-поля `goal.kpis` в полноценную Entity-сущность: `models/kpi.py` (`Kpi`, `entity_id` PK/FK + `goal_id` FK на `entity.id`, `target: float | None`, `unit`), `schemas/kpi.py` (`KpiRead`, задел под Шаг 3), `services/kpi_service.py` (create/list/delete-for-goal). Имя/описание KPI — в `entity.name`/`entity.description`, не дублируются в `kpi`.
  - `goal_service.compute_definiteness` переписан на сигнатуру `(owner, kpi_targets: list[float | None])`; `target=None` — валидный туман-кейс (KPI существует, таргета нет).
  - `create_goal`/`patch_goal` работают через `kpi_service`; патч `kpis` — replace-all (удалить прежние KPI цели, создать заново из payload), отмечено комментарием как временная стратегия.
  - API-контракт `Goal` не изменился (`kpis` по-прежнему список `{name, target, unit}` в create/read); внутри роут подгружает KPI-строки отдельным запросом и передаёт в `to_goal_read`.
  - Миграция `add kpi entity, drop goal.kpis json`: создана таблица `kpi` (FK ×2 на `entity.id`), дропнута колонка `goal.kpis`; данных в dev не было — data-migration не потребовалась. Известный автоген-quirk (`Text`-импорт для JSONB-варианта) поправлен в downgrade для симметрии с исходной миграцией.
  - `kpi` зарегистрирован в импортах моделей (`alembic/env.py`, оба тестовых регистратора).
  - Тесты обновлены под новую модель: definiteness-ветка «KPI без target → fog», replace-all патч (1 KPI → 2 KPI), проверка «KPI — сущность со своим id». `pytest` (30), `ruff`, `ruff format`, `mypy` — зелёные.
- **Дальше:**
  - Смёржить ветку в `main` (правило — не копить ветки).
  - Шаг 2b — структурное дерево целей (`parent_id`); Шаг 3 — граф связей KPI→KPI + ресурсы узла.

## 2026-07-08 · main · интеграция веток, main консолидирован

- **Что сделано:** слиты в `main` в порядке зависимостей: (1) `docs/delegation-policy`, (2) `docs/goal-alignment-model` (тянет за собой `docs/reconcile-decisions` — отдельно не сливалась, коммиты уже внутри), (3) `feat/goal-entity-step1-crud`. Конфликты возникли только в `DEVLOG.md` (дважды, 3 записи объединены по фактическим таймстемпам коммитов) и `BACKLOG.md` (Шаг 2/3 Goal Entity — оставлена более детальная версия из ADR-0003 вместо устаревшего дубля); код и канонические доки (`Management_Model.md`, ADR-файлы, `*.py`) слились чисто, без конфликтов. Все влитые ветки удалены (`docs/delegation-policy`, `docs/goal-alignment-model`, `docs/reconcile-decisions`, `feat/goal-entity-step1-crud`) — в репозитории остаётся только `main`. Проверка после слияния кода: backend (`ruff check`, `ruff format --check`, `mypy` — все зелёные, `pytest` — 27 passed) и frontend (`npm run build`, `npm run lint`) — все зелёные.
- **Дальше:** `main` содержит политику делегирования (`CLAUDE.md`), ADR-0002+§7, ADR-0003+§9, код Goal Шага 1. Следующий срез — Шаг 2a (вынос KPI из `goal.kpis`-JSON в сущность, рефактор `compute_definiteness`), см. `BACKLOG.md`. Push не делался — только по явному указанию владельца.

## 2026-07-08 · docs/goal-alignment-model

- **Что сделано:** зафиксирована в каноне модель KPI и увязки целей, обсуждённая до кода Шагов 2–3: KPI — Сущность со стабильным ID (не JSON-поле); у узла дерева индивидуальная цель (собственный KPI) или командная (ссылка на конкретный вышестоящий KPI, не копия); два независимых отношения — структура (`parent_id`, «содержит») и граф смысловых связей (KPI → KPI, может вести к любому вышестоящему KPI, не только к структурному родителю); связь создаётся сверху вниз и снизу вверх; два вида сиротства подсвечиваются (инициатива без связи вверх / KPI без исполнителя снизу). Добавлен `Management_Model.md` §9 (Ф8) с каноническим примером и Mermaid-диаграммой (структура vs граф связей — разными типами рёбер), уточнены §1.1/§2 (структура ≠ смысл, без переписывания существующих примеров), зафиксированы оставшиеся напряжения (название раздела 1 «декомпозиция», две независимые оси агрегации ресурсов/увязки). Создан `docs/adr/0003-kpi-entity-and-goal-alignment.md` (Accepted, модель) с разбивкой на Шаги 2a/2b/3 и открытыми вопросами (множественные связи, удаление KPI-источника, направленность/циклы графа, измерение качественных KPI) — без домысленных решений. Синхронизированы `docs/adr/README.md`, `docs/full-vision/INDEX.md`, `BACKLOG.md` (Шаги 2a/2b/3 расписаны, Шаг 1 отмечен выполненным).
- **Дальше:** Шаг 2a (вынос KPI из `goal.kpis`-JSON в сущность, рефактор `compute_definiteness`) — следующий кодовый срез backend. Ветка `docs/goal-alignment-model` создана от `docs/reconcile-decisions` (не от `main`), т.к. `main` ещё не содержит слияния ADR-0002 — при мёрже учитывать порядок веток (`docs/reconcile-decisions` → `docs/goal-alignment-model` → `main`), отдельно от кодовой ветки `feat/goal-entity-step1-crud`, которая ветвилась от `main` раньше и пока не содержит док-канона ADR-0002/0003.

## 2026-07-08 · feat/goal-entity-step1-crud

- **Что сделано:**
  - Реализован Goal Entity, Шаг 1: `models/goal.py`, `schemas/goal.py`, `services/goal_service.py`, `api/v1/goals.py` (CRUD) — по образцу Decision.
  - Автоматический расчёт определённости «туман/определён» (`compute_definiteness`): нужен owner + KPI с числовым таргетом; ручной флаг `is_backlog` — отдельное измерение, не влияет на расчёт.
  - Миграция `add goal table` сгенерирована и применена (`alembic upgrade head`); исправлен известный автоген-quirk (отсутствующий импорт `Text` для JSONB-варианта).
  - `goal` зарегистрирован в импортах моделей (`alembic/env.py`, оба тестовых conftest/test-регистратора).
  - Тесты: интеграционные (`test_goals.py`, все ветки definiteness + is_backlog) и юнит (`test_goal_service.py`, прямые тесты `compute_definiteness` + create/patch). `pytest`/`ruff`/`ruff format`/`mypy` — зелёные.
- **Дальше:**
  - Привязка frontend-карты целей (сейчас прототип на демо-данных) к реальному API — отдельный будущий срез (см. BACKLOG).
  - Шаг 2a/2b/3 по модели `docs/adr/0003-kpi-entity-and-goal-alignment.md` (эта ветка ветвилась от `main` до появления ADR-0002/0003 в доке-каноне; детализация задач — в BACKLOG).

## 2026-07-08 · docs/reconcile-decisions

- **Что сделано:** зафиксированы в каноне 4 из 7 открытых вопросов Management_Model §7 (Ф7) под скоуп первого кодового среза (Goal Entity + детерминированная увязка, без LLM): формулы увязки (арифметика), единицы измерения ресурсов (`people`/`money`/`other`), автодетекция «тумана войны»/«определён» + ручной флаг «бэклог», пересчёт на каждое изменение. Уточнены §3.1/§3.2/§3.3/§4 (пометки среза, диаграммы не сломаны). 3 вопроса (права ролей, версионирование справок, каталог навыков) явно отложены до соответствующих будущих срезов, без домысливания. Создан `docs/adr/0002-reconciliation-first-slice.md` (Accepted). Синхронизированы `docs/adr/README.md`, `docs/full-vision/INDEX.md`, `BACKLOG.md`.
- **Дальше:** проектирование модели данных Goal Entity на бэкенде может опираться на ADR-0002 (поля `resource.kind`, состояние определённости). §7 вопросы 3/5/6 остаются в BACKLOG, привязаны к своим будущим срезам (роли/доступ, офисные агенты M7–M8, навыки-воркфлоу).

## 2026-07-08 · docs/delegation-policy

- **Что сделано:** добавлен раздел «Делегирование (субагенты)» в `CLAUDE.md` — политика лида: 4 контролёра из `.claude/agents/` авто-делегируются по `description`, роли-исполнители не заданы заранее (лид дробит на временные субагенты по контексту задачи, ориентир — 3–5 одновременно), кристаллизация в файл `.claude/agents/` только по факту повторяемости.
- **Дальше:** ничего специально не запланировано этим проходом; следовать политике при следующих задачах, требующих делегирования.

## 2026-07-07 · main · baseline

- **Что сделано:**
  - Наведён порядок в документации: убраны дубли PRD/Decision_Center/Entity_Platform, `docs/Command_Center.md` → `docs/full-vision/09_Design_System/Visual_Reference.md`, пересобран INDEX.md, зафиксирован канон модели управления `Management_Model.md` (Ф1–Ф7).
  - Chore-проход: поправлены битые пути в докстрингах backend, наполнен `docs/adr/` шаблоном и README.
  - Оформлен визуальный бренд-бук (Часть II `Visual_Reference.md`): токены, компоненты, семантика состояний — извлечено дословно из кода прототипа `frontend/src/os/`.
  - `ai-agents/` инициализирован как единый репозиторий-ОС: `decision-center/` (пилот) поднят в корень, его локальная история `.git` удалена (рабочий код сохранён), `FreeDeepseekAPI/` вынесен из воркспейса в `c:/Dev/FreeDeepseekAPI` (git-история цела) и зафиксирован как fallback-направление (`docs/adr/0001`), утверждённые дизайн-рендеры перенесены в `docs/full-vision/09_Design_System/renders/` (раздел D10 в бренд-буке), транзитный мусор (`.playwright-mcp/`, `frontend/src.rar`) удалён.
  - Именование зафиксировано: продукт/репозиторий — ОС `ai-agents`, «Decision Center» — имя первого среза (не продукта).
  - Claude Code настроен по максимуму: `CLAUDE.md` (память проекта), `.claude/rules/` (backend/frontend/docs/commits), `.claude/agents/` (code-reviewer, spec-guardian, doc-keeper, test-runner), `.claude/commands/` (`/devlog`, `/ship`, `/adr`), `.claude/hooks/` (protect-main, block-secrets, format-code, statusline) + `.claude/settings.json` (permissions/env/hooks/statusLine), `.mcp.json` (пустой каркас), `.claude/agent-memory/`.
- **Дальше:**
  - M3: борд из трёх персон (CFO/COO/CTO) + синтез.
  - Спайк по локальным LLM-моделям для fallback (`docs/adr/0001`, таблица кандидатов).
  - Решить финальное потребительское имя продукта (кандидат — «Вектор·OS»).
