# Command Center — интерфейсная модель и визуальный бренд-бук

**Версия:** 2.0
**Статус:** Product Definition (Часть I) + Design System as-built (Часть II)
**Связанные документы:** `02_Product/PRD.md`, `02_Product/Management_Model.md`, Entity_Platform.md, Decision_Center.md

Документ состоит из двух частей:

- **Часть I. Концепция** (разделы 1–13) — интерфейсная модель Command Center: карта целей как главный экран, уровни масштаба, юниты, блоки-районы, сквозные агенты.
- **Часть II. Дизайн-система (as-built)** (разделы D1–D9 + «Открытые вопросы») — визуальный бренд-бук, извлечённый из кода рабочего прототипа.

> ⚠ **Правило Части II:** она документирует то, что УЖЕ реализовано в коде (`frontend/src/index.css`, `frontend/src/os/os.css`, компоненты `frontend/src/os/`), и обязана коду соответствовать. Код — источник истины; при расхождении верить коду и фиксировать расхождение в «Открытых вопросах», а не «улучшать» доку. Приоритет источников: `02_Product/PRD.md` → `02_Product/Management_Model.md` → этот документ (визуал).

---

# Часть I. Концепция

---

# 1. Назначение

Command Center — интерфейсная модель Enterprise Operating System.

Управление компанией строится не как набор форм и списков, а как панель управления стратегической игры (в духе Civilization): карта, уровни масштаба, юниты, ходы.

Пользователь не «заполняет CRUD», а играет за свою компанию.

---

# 2. Основной принцип

Компания — это играбельная карта.

Главный экран системы — **карта целей компании**: визуализация Entity Graph в срезе целей (Goal), их связей, состояния и прогресса.

Любой другой экран — либо более детальный масштаб этой карты, либо меню настройки объектов на ней. Формы вторичны и открываются из контекста карты.

---

# 3. Уровни масштаба

Как в стратегической игре: мир → регион → город → юнит.

```

Компания
(карта целей организации)

↓

Подразделение
(карта целей отдела — деталь карты компании)

↓

Сотрудник
(личная карта: его цели, задачи, его агенты)

↓

Агент
(настройка, обучение, метрики конкретного AI-агента)

```

Каждый уровень — тот же Entity Graph, отфильтрованный по зоне ответственности.

Основной экран сотрудника — карта целей его отдела: он видит свой участок «большой карты», а не абстрактный список задач.

---

# 4. Карта целей

## Узлы

Каждая цель — Entity типа Goal.

Цели делятся на два контура:

- **внешние** — рынок, клиенты, выручка, продукт;
- **внутренние** — процессы, компетенции, инфраструктура, учёт.

## Связи

Связи целей — стандартные отношения Entity Platform:

- decomposes (цель компании → цели отделов → цели сотрудников);
- depends_on;
- blocks;
- supports;
- conflicts.

## Индикаторы на карте

Каждый узел показывает:

- Health Score;
- прогресс по KPI;
- уровень риска;
- ответственного (сотрудник или агент);
- привязанные решения (Decision).

## Ход

Решение (Decision) — это ход на карте. Каждое решение привязано к цели, которую оно двигает. Жизненный цикл решения (Decision_Center.md, раздел 3) отображается прямо на узле цели.

---

# 5. Меню оргструктуры

Руководитель не собирает оргструктуру вручную из форм. Структуру выстраивает Советник — сквозной AI-агент (раздел 8).

## Порядок работы

```

Руководитель описывает простыми словами
цели и задачи компании / подразделения

↓

Советник выстраивает оргструктуру:
подразделения, должности, должностные инструкции,
KPI, связи с картой целей

↓

Руководитель просматривает, корректирует, утверждает
(утверждение структуры — Decision)

↓

Под утверждённые должности назначаются сотрудники

```

Советник создаёт для каждой должности:

- должностную инструкцию;
- требуемый профиль компетенций;
- KPI должности, увязанные с целями подразделения;
- место в структуре подчинения (reports_to).

## Настройка сотрудника

Сотрудник (физический человек) — Entity типа Human Employee, назначаемая на должность.

При назначении сотрудника система **автоматически ставит ему первичную задачу — «Оценка знаний и навыков»**:

- прохождение оценки заполняет профиль компетенций (Skill / Competency Entities);
- профиль сравнивается с требуемым профилем должности — разрыв становится планом развития;
- профиль определяет, какие цели и задачи ему можно назначать;
- профиль — стартовая точка для роста (Maturity сотрудника как Entity).

После оценки сотрудник появляется на карте своего отдела как юнит с характеристиками.

---

# 6. Универсальные блоки управления

Помимо оргструктуры панель содержит стандартный набор блоков управления, применимых к любой фирме и любому проекту. Каркас блоков предустановлен исходя из лучших практик; наполнение под конкретную компанию выполняет Советник.

## Каталог блоков

| Блок | Содержимое | Контур |
|---|---|---|
| Стратегия и цели | карта целей (раздел 4), OKR, KPI | мидл |
| Финансы | бюджет, cash flow, управленческий учёт, юнит-экономика, прогноз | мидл |
| Бухгалтерия и налоги | регламентированный учёт, первичные документы, отчётность, налоги | бэк |
| Персонал (HR) | найм, онбординг, кадровый учёт, оценка, мотивация, преемственность | бэк |
| Корпоративный университет | курсы, аттестации, матрица компетенций, траектории развития — для людей **и** агентов | сквозной |
| Продажи и клиенты (CRM) | лиды, сделки, воронка, договоры с клиентами | фронт |
| Маркетинг | кампании, каналы, бренд, аналитика привлечения | фронт |
| Операции | производство / оказание услуг, процессы, проекты, задачи | бэк |
| Закупки и поставщики | поставщики, заказы, запасы, активы | бэк |
| Юридический блок | договоры, политики, комплаенс | мидл |
| ИТ и безопасность | системы, интеграции, доступы, данные | бэк |
| Знания и документы | база знаний, SOP, документооборот, организационная память | сквозной |
| Риски | реестр рисков, мониторинг, реагирование | мидл |

Полный каталог возможностей — `full-vision/07_Business_Capabilities/Business_Capability_Map.md`; блоки Command Center — его пользовательская проекция на игровую панель.

## Принципы блоков

- **Блок — район карты.** У блока свои внутренние цели, KPI, юниты и агенты; на карте компании блок виден как район со своим Health Score.
- **Активация по зрелости.** Не все блоки включаются в первый день. Блок активируется, когда появляется потребность — как строительство здания в стратегии. Рекомендацию «пора строить» даёт Советник.
- **Одинаково для фирмы и проекта.** Проект получает те же блоки в мини-масштабе: бюджет проекта, команда проекта, риски проекта, знания проекта.

## Корпоративный университет

Единая среда обучения Unified Workforce — людей и агентов:

- здесь проходит первичная «Оценка знаний и навыков» (раздел 5) и закрываются выявленные ею разрывы;
- здесь живут планы развития сотрудников и траектории роста компетенций;
- здесь же сотрудники обучают своих агентов (раздел 7): материалы, датасеты, аттестация агентов перед повышением автономности.

---

# 7. Слой агентов

За каждым сотрудником закреплены AI-агенты — его личная команда (AI Employee Entities, связь `owns` / `reports_to`).

## Сотрудник — наставник своих агентов

За обучение агентов отвечает сотрудник:

- формирует контекст и знания агента (Entity Knowledge);
- даёт обратную связь по результатам;
- принимает или отклоняет работу агента;
- решает, когда повышать автономность.

## Рост агента

Агент развивается по шкале зрелости Entity Platform:

Initial → Developing → Managed → Optimized → Autonomous

Рост зрелости открывает рост автономности (Manual → Assisted → Supervised AI → Autonomous AI). Метрики агентов видны на карточке сотрудника-наставника: качество, скорость, стоимость, процент принятых работ.

---

# 8. Сквозные агенты (офисные контуры)

Поверх вертикали «компания → отдел → сотрудник → агент» работают сквозные агенты. Они не принадлежат отделам — они следят за целостностью всей карты.

## Советник

Организационный контур: структура, роли, инструкции, KPI.

Принимает от руководителя описание целей и задач простыми словами и выстраивает оргструктуру (раздел 5): подразделения, должности, должностные инструкции, требуемые профили компетенций, KPI. Разворачивает и наполняет универсальные блоки управления (раздел 6), рекомендует активацию новых блоков. При изменении карты целей предлагает изменения структуры.

## Фронт-офис агенты

Внешний контур: клиенты, рынок, продажи, продукт.

Следят за внешними целями: реалистичность, актуальность, сигналы рынка. Курируют блоки фронт-контура: CRM, маркетинг.

## Бэк-офис агенты

Внутренний контур: процессы, финансы, учёт, инфраструктура.

Следят за внутренними целями: обеспеченность ресурсами, исполнимость, эффективность. Курируют блоки бэк-контура: бухгалтерия, HR, операции, закупки, ИТ.

## Мидл-офис агенты

Контур увязки: риски, координация, соответствие.

Главная функция — **взаимоувязка внешних и внутренних целей**:

- каждая внешняя цель должна опираться на внутренние цели, которые её обеспечивают;
- каждая внутренняя цель должна прослеживаться к внешней ценности;
- разрыв, конфликт или рассинхронизация — это находка.

Курируют блоки мидл-контура: финансы, юридический блок, риски.

## Сигнал → Решение

Находка сквозного агента автоматически создаёт Decision в стадии «Обнаружено» (Decision_Center.md, раздел 3) с привязкой к затронутым целям.

Сквозные агенты — двигатель стадии «Обнаружение» жизненного цикла решений.

---

# 9. Игровые механики

Механики — стратегические, а не «баллы и бейджи»:

- **Ход** — решение (Decision), утверждённое и исполненное.
- **Юниты** — сотрудники и их агенты, с характеристиками из профилей компетенций и метрик.
- **Ресурсы** — бюджет, время, внимание руководителя.
- **Здания и районы** — универсальные блоки управления (раздел 6), активируемые по мере зрелости компании.
- **Туман войны** — зоны карты без данных: цели без KPI, отделы без метрик, связи без владельца, неактивированные блоки.
- **Технологическое древо** — рост компетенций сотрудников и зрелости агентов открывает новые уровни автономности.

Документ `full-vision/04_Simulation/Game_Mechanics.md` (XP, бейджи, лидерборды) описывает потребительскую геймификацию и **не является** основой Command Center.

---

# 10. Маппинг на Entity Platform

Всё, что видно на панели, — Entity:

| Элемент панели | Entity |
|---|---|
| Узел карты целей | Goal |
| Подразделение | Department / Team |
| Сотрудник | Human Employee |
| Агент сотрудника | AI Employee |
| Сквозной агент | AI Employee (контур: advisor/front/middle/back) |
| Должность и должностная инструкция | Entity (создаются Советником) |
| Блок управления | Entity-домен, объединяющий цели, KPI и процессы блока |
| Профиль навыков | Skill / Competency |
| Первичная оценка | Task |
| Курс, аттестация | Course / Skill (блок «Университет») |
| Ход | Decision |

Карта — представление Entity Graph. Новых моделей данных вне Entity Platform не создаётся (Entity_Platform.md, раздел 20).

---

# 11. MVP интерфейса

Поэтапно, каждый шаг — играбельный:

1. **Карта целей компании** (чтение): узлы-цели, связи, привязанные решения. Данные — Goal Entity + существующие Decision.
2. **Меню оргструктуры через Советника**: руководитель описывает цели простыми словами → Советник генерирует структуру с должностями, инструкциями и KPI → утверждение; карточка сотрудника, автозадача «Оценка знаний и навыков».
3. **Слой агентов**: привязка агентов к сотрудникам; первыми агентами выступают персоны AI Board (CFO/COO/CTO).
4. **Сквозные агенты**: сначала как детерминированные проверки увязки целей (без LLM), затем как полноценные агенты.

Каркас блоков управления (раздел 6) предустановлен с первого шага, но блоки подключаются постепенно — первыми активируются «Стратегия и цели» и «Оргструктура», остальные по мере милстоунов и зрелости продукта.

Текущие экраны (список решений / форма / деталь) — временный каркас, они становятся деталями карты, а не главным интерфейсом.

---

# 12. Future

- Симуляция ходов на карте перед их выполнением (связь с Simulation Engine).
- Автономные ходы агентов в пределах политик.
- Реиграбельность: сценарии «что если» на копии карты.
- Мультиплеер: несколько руководителей на одной карте с зонами ответственности.

---

# 13. Назначение документа

Command Center определяет интерфейсную модель Enterprise Operating System: компания управляется через карту целей со стратегическими уровнями масштаба, сотрудники и их AI-агенты являются юнитами, решения — ходами, а универсальные блоки управления (финансы, бухгалтерия, HR, корпоративный университет и другие) — районами карты, активируемыми по мере зрелости. Советник выстраивает оргструктуру и блоки по описанию целей простыми словами, а сквозные агенты фронт-, мидл- и бэк-офиса непрерывно следят за взаимоувязкой внешних и внутренних целей и порождают новые решения.

---
---

# Часть II. Дизайн-система (as-built)

Формализация визуальной системы, реализованной в прототипе двух экранов: **панель управления** (`.os-panel`, `CommandPanel.tsx`) и **карточка цели** (`.os-goal`, `GoalCard.tsx` + `ProcessMap.tsx`). Все значения ниже взяты дословно из `frontend/src/index.css` (токены, база, анимации) и `frontend/src/os/os.css` (каталог компонентов); демо-данные и логика состояний — `frontend/src/os/data.ts` и `frontend/src/os/goals.ts`.

---

# D1. Идентичность и тон

Рабочие имена визуального языка, зафиксированные в коде:

- **Terminal Strategist** — название эстетики (комментарий в шапке `index.css`: «Вектор·OS — Terminal Strategist base (per approved design renders)»).
- **Вектор·OS** — подпись продукта в интерфейсе (логотип в топ-баре `CommandPanel.tsx`, `<title>` в `frontend/index.html`: «Вектор·OS — Панель управления»).

Тон: **стратегический командный центр**. Единственная тема — тёмная (`color-scheme: dark`). Моноширинные «терминальные» акценты для всего численно-кодового; сдержанная геймификация — ходы, туман войны, здоровье блоков, но без конфетти, XP и бейджей (согласуется с Management_Model §8 п. 4 и Частью I §9: потребительская геймификация `Game_Mechanics.md` — НЕ основа Command Center).

> **Именование (решение 2026-07-07).** Продукт и репозиторий — **ОС `ai-agents`** (операционная система по управлению агентами и фирмой). **«Decision Center» — имя первого вертикального среза**, не продукта. «Вектор·OS» — рабочий алиас в коде прототипа и кандидат в финальное имя (финал за владельцем); «Command Center» — название интерфейсной модели (Часть I). «AI Company Simulator» в PRD/INDEX — историческое имя концепции из full-vision-архива.

---

# D2. Цветовые токены

Все токены объявлены в `index.css :root`. Семантика — по фактическому использованию в `os.css`.

## Фон и поверхности

| Токен | Значение | Семантика |
|---|---|---|
| `--bg` | `#0a0a0b` | Фон приложения и полотна карты (`.os-panel`, `.stage`, `.os-goal`) |
| `--bar` | `#0e0e10` | «Хром»: верхний и нижний бары, левый rail, панель советников |
| `--sf` | `#141416` | Поверхность карточек: `.gcard`, `.slot`, `.msg.ai`, `.ch`, `.rc`, `.flow`, `.panel`, `.pmap` |
| `--sf2` | `#1b1b1e` | Приподнятая поверхность: аватар, активная кнопка rail, `.bitab`, `.chip`, `.msg.me`, мелкие кнопки |
| `--ln` | `rgba(255, 255, 255, 0.08)` | Универсальная линия: бордеры 1px, разделители, подложки прогресс-баров |

## Чернила (текст)

| Токен | Значение | Семантика |
|---|---|---|
| `--ink` | `#e4e3df` | Основной текст; тем же hex захардкожен маркер человека (`HumanDot`) |
| `--i55` | `rgba(228, 227, 223, 0.55)` | Вторичный текст: описания, неактивные пункты, «свои» сообщения |
| `--i40` | `rgba(228, 227, 223, 0.4)` | Подписи, метки, ID, футеры карточек |
| `--i35` | `rgba(228, 227, 223, 0.35)` | Самые тихие подписи: капс-метки, placeholder, ожидающие состояния |

## Семантические цвета

| Токен | Значение | Семантика |
|---|---|---|
| `--gr` | `#8fd14f` | Зелёный: здоровье, выполнено, подтверждение, AI-ok, фокус-обводки, акцентные кнопки |
| `--op` | `#e8c04a` | Жёлтый (внимание): активная цель, текущий шаг, warn-сигналы, маркер AI-юнита (`AiDot`) |
| `--rk` | `#e8544a` | Красный: риск, перегруз, дефицит, связь «блокирует» |

Прочее в `:root`: базовый шрифт `font: 400 14px/1.5 var(--sans)`; скроллбар — `#2b2b2f` (`scrollbar-color`), `#26262a` (webkit thumb).

## Долг консистентности: цвета мимо токенов

В `os.css` и компонентах часть цветов захардкожена в обход токенов. **Фиксируем, НЕ чиним в этом проходе** (полный список — в «Открытых вопросах»):

- `#161618` — поле поиска, инпуты чата/советника, плитка-иконка `.hd .ic`;
- `#101012` — «углублённые» поверхности: `.gx`, `.ch.cur`, `.amsg`, `.rpool`, `.pm-box`;
- `#1e2117` — зелёно-тонированный фон акцентных кнопок и бейджей: `.inp button`, `.b-act`, `.abtn .pri`, `.rtog button.on`, `.actions .ok`, `.chip.ai`;
- `#181a16` (`.slot.on`), `#131510` (`.opt.on`) — зелёно-тонированные активные состояния;
- `#141416` в `.pm-gate` и `#0a0a0b` в `stroke` у `.pm-cond` — дублируют значения `--sf` и `--bg` литералом;
- многочисленные rgba-производные от семантических hex (`rgba(143,209,79,.4)`, `rgba(232,192,74,.45)`, `rgba(232,84,74,.12)`, `rgba(228,227,223,.25)` и т.п.) — прозрачность задаётся литералом, а не через токен;
- в TSX: `#8fd14f`, `#e8c04a`, `#e8544a`, `#e4e3df`, `STROKE = 'rgba(228,227,223,.55)'`, `NODE_STROKE` (`ProcessMap.tsx`), цвета связей карты целей (`CommandPanel.tsx`).

---

# D3. Типографика

## Семейства

| Токен | Стек | Правило применения |
|---|---|---|
| `--sans` | `'IBM Plex Sans', 'Segoe UI', system-ui, sans-serif` | Контент: названия целей и этапов, описания, чат, кнопки-действия |
| `--mono` | `'IBM Plex Mono', 'Cascadia Mono', Consolas, monospace` | Числа, ID/коды (`GOAL-014`), метрики, KPI, капс-метки, метки состояний, хлебные крошки |

Начертания подключены в `frontend/index.html` (Google Fonts): Sans 400/500/600/700, Mono 400/500/600. Капс-метки набираются mono с разрядкой `letter-spacing: 0.03em–0.14em` (максимум — `.bitab` 0.14em).

## Фактическая шкала (из `os.css`; font-shorthand дословно)

| Роль | Font | Где применяется |
|---|---|---|
| Заголовок карточки цели | `700 22px var(--sans)` | `.os-goal .hd h1` |
| Крупный процент | `600 20px var(--mono)` | `.gx .hd .pct b` |
| KPI-значение | `600 19px var(--mono)` | `.os-goal .hd .k .v` |
| Значение ресурса | `600 18px var(--mono)` | `.rc .v` |
| Заголовок раскрытой цели; стрелки цепочки | `600 15px var(--sans)`; `600 15px var(--mono)` | `.gx .hd .t`; `.arr` |
| Базовый текст | `400 14px/1.5 var(--sans)` | `:root` (index.css) |
| Название этапа | `600 13.5px var(--sans)` | `.wstage .nm` |
| Логотип; названия узлов; кнопки-действия | `600 13px var(--mono)` / `600 13px var(--sans)` | `.logo`; `.gcard .nm`, `.ch .nm`; `.actions button` |
| Чат, сообщения советника, инпуты, опции | `400 12.5px/1.55 var(--sans)` (инпуты/опции без line-height) | `.msg`, `.amsg`; `.inp input`, `.opt` |
| Рабочий текст и метки | `400/500 12px var(--sans)` и `var(--mono)` | `.crumb`, `.search`, `.rail button`, `.srow`, `.wstage .dsc`, `.check`, `.rrow`, `.cl`, `.inp button`, `.ainp button`, `.gx .hd .s` |
| Мелкие метрики | `500/600 11.5px` | `.top .r`, `.bot .m` (mono); `.slot .sn`, `.who .p`, `.abtn button` (sans); `.note`, `.ovl` (400 sans); `.rload b` (600 mono) |
| Капс-метки, бейджи, номера | `500/600 11px var(--mono)`; `600 11px var(--sans)` | `.maplbl`, `.bdg`, `.adv .ph .t`, `.panel .ph .t`, `.check .m`, `.num`, `.ava`, `.due`, `.addstage`, `.lnk`; `.pm-name` |
| Тихие подписи | `500 10.5px var(--mono)` | `.os-goal .cap`, `.gx .ft`, `.st`, `.rtog button`, `.rcost`, `.radd`, `.hint`, `.rail button .hp` |
| Микро-метки | `500 10px var(--mono)` (`.bitab` — 600) | `.rail .cap`, `.gcard .ft`, `.ch .ft`, `.k .l`, `.srow .who`, `.quick button`, `.ctx`, `.chip`, `.fog`, `.pm-legend`, `.bitab` |
| Идентификаторы | `500 9.5px var(--mono)` | `.gcard .id`, `.ch .id`, `.pct span`, `.who .r`, `.scode`, `.rkind`, `.rpool em`, `.opt .hint` |
| Мета чата, номера шагов, мета узлов | `500 9px var(--mono)` (`.srow .n` — 600) | `.msg .mt`, `.amsg .mt`, `.slot .sr`, `.pm-meta`, `.srow .n`; SVG-подписи связей карты целей — `fontSize 9`, IBM Plex Mono (`CommandPanel.tsx`) |
| Условия переходов процесса | `500 8.5px var(--mono)` | `.pm-cond` |

---

# D4. Сетка, отступы, форма

## Скругления

- **Доминанта — `4px`**: карточки, кнопки, инпуты, бейджи, панели, узлы карты процесса (`rx="4"`).
- `3px` — мелкое: чипы, quick-кнопки, `.rtog`/`.rload` кнопки, `.radd`, `.ctx i`; `.ovl` — `0 3px 3px 0` (срез под левый акцент-бордер).
- `2px` — прогресс-бары (`.bar`, `.tr`).
- `50%` — исключения-круги: точки `.dot`, аватар `.ava`, номера шагов `.srow .n` / `.num`, радио `.opt .rad`.
- `.bitab` — `4px 0 0 4px` (прижата к правому краю).

## Линии и бордеры

- Стандарт: `1px solid var(--ln)`; внутренние разделители строк — `rgba(255,255,255,.05–.06)`.
- Утолщённые: `1.4px` (`.srow .n`; stroke `.pm-box`, `.pm-gate`), `1.5px` (`.num`, `.hd .ic`, `.opt .rad`; stroke `.pm-edge`), `1.6px` (stroke SVG-иконок и связей карты целей), `2px` (юнит-точки, `.pm-box` в hot-состоянии, фокус-обводка).
- **Пунктир = «черновик / следующее / нет данных»**: `1px dashed rgba(228,227,223,.25)` (`.g-next`, `.ch.next`, `.radd`), `.15` (`.fog`); `.slot.add` — `border-style: dashed`; в SVG — `stroke-dasharray: 5 6` (активные потоки), `4 5` (`.pm-edge.pm-next`), `5 4` (рамка узла-next).
- Левый акцент-бордер `3px solid` — код состояния карточки: `.g-done`/`.g-warn`/`.g-risk`, `.ch.prev`, `.msg.ai`, `.amsg`, `.ovl`.

## Ключевые раскладки (размеры из `os.css`)

**Панель управления `.os-panel`** — колонка на всю высоту:
- топ-бар `.top` — высота `56px`, паддинг `0 20px`; поиск `.search` — ширина `320px`;
- `.shell`: rail `178px` (флекс-колонка, паддинг `14px 10px`) + сцена `.stagewrap` (flex 1) + советники `.adv` `352px`;
- полотно карты `.canvas` — `1040×560px`, по центру; фон сцены — двухслойный радиальный «звёздный» паттерн (`34px`/`46px`) поверх `--bg`;
- нижний бар `.bot` — высота `44px`; трек здоровья `.tr` — `110×4px`.

**Карточка цели `.os-goal`**:
- топ-бар `56px`, sticky;
- `.wrap` — `max-width: 1480px`, паддинг `24px 28px 40px`, вертикальный `gap: 22px`;
- чейн-полоса `.chain` — грид из 5 колонок `1fr 34px 1.35fr 34px 1fr` (карточка → стрелка → текущая (шире) → стрелка → карточка);
- `.cols` — грид `1fr 350px`, `gap: 22px` (основная колонка + сайдбар);
- ресурсы `.res` — грид `repeat(3, 1fr)`, `gap: 12px`; `.rc2` — на всю ширину, `.rcw` — 2 колонки;
- строка этапа `.wstage` — грид `30px 1fr 210px 130px 90px`, `gap: 14px` (номер / контент / ответственные / срок / статус);
- адаптив `@media (max-width: 1100px)`: `.cols` и `.chain` в одну колонку, `.res` — `1fr 1fr`, стрелки `.arr` скрыты, `.wstage` — `30px 1fr`.

**Карта процесса** (константы `ProcessMap.tsx`): узел `172×56px` (`W`/`H`), шаг сетки `216×92px` (`COLW`/`ROWH`), гейт — ромб `12×12` на отступе `22px` от узла; укладка по уровням — longest path от истоков.

---

# D5. Каталог компонентов

Все классы — из `os.css`; место рендера — `CommandPanel.tsx` (панель), `GoalCard.tsx` (карточка), `ProcessMap.tsx` (карта процесса).

## Карта целей / узлы (панель управления)

| Класс | Назначение и состояния |
|---|---|
| `.gcard` | Узел-цель на карте: ID+тип (`.id`), название (`.nm`), прогресс-бар (`.bar i`), футер (`.ft`). Кнопка — клик открывает карточку цели. Hover — жёлтая обводка |
| `.g-done` / `.g-warn` / `.g-risk` | Левый акцент-бордер 3px: зелёный / жёлтый / красный; тем же цветом красится заливка прогресс-бара |
| `.g-next` | Планируемая цель: пунктирная рамка, приглушённые чернила, пустой бар |
| `.gx` | Раскрытая цель в фокусе: фон `#101012`, жёлтая обводка + свечение `box-shadow 0 0 0 4px rgba(232,192,74,.07)`; шапка `.hd` (иконка, название `.t`, подпись `.s`, процент `.pct`), бар, список этапов `.stg` со строками `.srow` (`.done`/`.act` — цвет номера `.n` и статуса `.st`), футер `.ft` со ссылкой `.open` |
| `.fog` | «Туман войны»: пунктирная зона `НЕТ ДАННЫХ` (Management_Model §3.3) |
| `.bitab` | Свёрнутая вертикальная вкладка «СВОДКА · BI» у правого края сцены |
| `.rail button` | Блок фирмы в левом rail: иконка, имя, здоровье `.hp`. Состояния: `.on` (активен, зелёная обводка), `.warn`/`.risk` (цвет цифры здоровья), `.off` (неактивный блок, opacity .38) |

## Карточка цели

| Класс | Назначение и состояния |
|---|---|
| `.hd` | Шапка: плитка-иконка `.ic` (48×48, жёлтая обводка), `h1`, подпись `.s`, KPI-плитки `.kpis > .k` (метка `.l` + значение `.v`, min-width 120px) |
| `.bdg` | Бейджи топ-бара: `.b-act` (зелёный, активный статус) / `.b-sec` (нейтральный, видимость) |
| `.chain` / `.ch` | Чейн-полоса «предыдущая → текущая → следующая»: `.ch.prev` (зелёный акцент-бордер), `.ch.cur` (фон `#101012`, жёлтая обводка + свечение), `.ch.next` (пунктир); анатомия узла как у `.gcard`; стрелки `.arr` |

## Ресурсы (единая рабочая сила, PRD §28)

| Класс | Назначение и состояния |
|---|---|
| `.caprow` + `.rtog` | Заголовок секции с переключателем вида: `Свои` / `Свёртка ветки` (`.rtog button.on` — зелёный) |
| `.rc` | Ресурсная карточка: метка `.t .l`, статус `.t .st`, значение `.v`, бар, примечание `.note`; `.rc2` — на всю ширину, `.rcw` — двойная |
| `.rrow` | Строка назначенного юнита: маркер типа, имя `.rname`, тип `.rkind`, загрузка `.rload` (кнопки ±, шаг 5%, диапазон 5–100), стоимость `.rcost`, снятие `.rdel` |
| `.radd` + `.rpool` | Кнопка «+ назначить юнит» и единый пикер людей и AI (один список `UNITS`: human/digital/team/dept; max-height 180px) |
| `.ovl` | Сигнал перегруза (PRD §41.1, логика `findOverloads()` в `goals.ts`): красный акцент — этапы требуют юнита больше, чем выделено родителю; `.ovl.warn` (жёлтый) — юнит занят на этапах, но вовсе не выделен узлу |
| `.chip` | Чип: базовый нейтральный; `.ai` (жёлтый на зелёно-тёмном — работа AI), `.miss` (красный — пробел: «KPI этапа не задан») |

## Этапы-цели

| Класс | Назначение и состояния |
|---|---|
| `.flow` | Контейнер списка этапов |
| `.wstage` | Строка этапа — кликабельная (`role="button"`, `tabIndex=0`, Enter), открывает этап той же карточкой (этап — сам по себе цель). Состояния: `.done` / `.act` (цвет номера `.num`, у `.act` — жёлтое свечение), `.hot` (синхронная подсветка с картой процесса: жёлтый фон + inset-полоса), вертикальный коннектор между номерами (`::after`, у `.done` зелёный) |
| `.chip.lc-*` | Lifecycle-чипы: `.lc-draft` (серый) / `.lc-active` (жёлтый) / `.lc-review` (нейтральный) / `.lc-archived` (зелёный); подписи — `LIFECYCLE_LABEL` в `goals.ts` |
| `.addstage` | Строка «+ добавить этап · или попросите советника…» |

## Карта процесса `.pmap` (BPMN-подобный граф, Management_Model §1.2)

| Класс | Назначение и состояния |
|---|---|
| `.pm-node` | Узел-этап (SVG `g`, кликабелен, `role="button"`): рамка `.pm-box`, название `.pm-name`, мета `.pm-meta` (KPI % + lifecycle). Состояния `pm-passed` / `pm-now` / `pm-next` (цвет обводки из `NODE_STROKE`, у next — пунктир), `.hot` — жёлтая обводка + drop-shadow, синхронно со строкой `.wstage` |
| `.pm-pulse` | Жёлтая пульсирующая точка на текущем узле |
| `.pm-edge` + `.pm-arr` | Ребро (кривая Безье) + стрелка: `.pm-passed` (зелёное, статичное), `.pm-now` / `.pm-ready` (жёлтое, пунктир, анимация потока), `.pm-next` (тусклый пунктир) |
| `.pm-gate` | Шлюз-ромб: деление (split) у истока, слияние (join) у стока — `OsTransition.kind` из `data.ts` (Management_Model §1.2) |
| `.pm-cond` | Подпись условия на переходе / режима слияния («ждать всех» / «любая ветка» — `mode: 'all' | 'any'`); жёлтая, с тёмной обводкой для читаемости |
| `.pm-legend` | Легенда под картой |

## Советники

| Класс | Назначение и состояния |
|---|---|
| `.adv` | Правая панель советников (352px): шапка `.ph`, слоты, контекст, чат, quick-кнопки, ввод |
| `.slot` | Слот советника (`ADVISOR_SLOTS`: Финансист, Стратег): имя `.sn` + роль `.sr`; `.on` — выбран (зелёный), `.add` — пустой слот (пунктир) |
| `.ctx` | Строка контекста: `КОНТЕКСТ` + жёлтая плашка с текущей целью |
| `.msg` | Сообщение чата: `.ai` (слева, зелёный акцент-бордер) / `.me` (справа, `--sf2`); мета `.mt`, выделение `.hl` (жёлтый), ссылка-действие `.lnk` (зелёная) |
| `.quick` | Быстрые кнопки-подсказки (hover — зелёный) |
| `.inp` | Ввод: инпут + зелёная кнопка отправки |
| `.panel` / `.amsg` / `.ainp` / `.abtn` | Сайдбар карточки цели: панель советника по постановке, его сообщение, ввод вопроса, кнопки `.pri` (зелёная) / `.ghost` |

## Проверки увязки

| Класс | Назначение и состояния |
|---|---|
| `.checks` / `.check` | Чек-лист советника по постановке цели (динамический, строится в `GoalCard.tsx`; статический образец — `advisorChecks` в `data.ts`): `.ok` (зелёный ✓) / `.wr` (жёлтый ⚠) / `.er` (красный ✕). Реализует увязку Management_Model §3: измеримость, связность, перегрузы, этапы без KPI/ответственного |

## Бейджи, чипы, аватары, точки

| Класс | Назначение |
|---|---|
| `.ava` | Круглый аватар 30×30, зелёные инициалы моно |
| `.dot` | Точка-индикатор 7×7 (`+ .pulse` — пульсирует) |
| `HumanDot` / `AiDot` | Маркеры юнитов (SVG в TSX): человек — круг цвета чернил, AI — ромб `#e8c04a`; `UnitMark` относит `team`/`dept` к «человеческому» маркеру |
| `.opt` / `.rad` / `.grant` | Опции видимости (радио-список, `.on` — зелёный) и чипы текущего доступа |
| `.actions .ok` / `.dr` | Финальные действия: «Утвердить цель» (зелёная) / «Сохранить черновик» (ghost) |

---

# D6. Семантика состояний

Единая таблица «состояние → визуальный код → смысл». Канон: PRD §14.3 (жизненный цикл), Management_Model §3.3 (определённость), PRD §41.1 (измеримость/перегруз).

| Домен | Состояние | Визуальный код | Смысл / источник |
|---|---|---|---|
| Lifecycle (PRD §14.3) | `draft` — Черновик | `.chip.lc-draft`: серый (`--sf2` + `--i40`) | Узел создан, не запущен |
| | `active` — Активна | `.chip.lc-active`: жёлтый | В работе |
| | `review` — На проверке | `.chip.lc-review`: нейтральный | Ждёт приёмки |
| | `archived` — Архивирована | `.chip.lc-archived`: зелёный | Завершена |
| Поток процесса (`flowState()` в `goals.ts`) | `passed` (= archived) | Зелёное: погашенный узел, статичное зелёное ребро, `✓`, `ГОТОВО` | Пройдено |
| | `now` (= active/review) | Жёлтое: обводка, пульс (`os-pulse`), бегущий пунктир ребра (`os-flow`), `● В РАБОТЕ` | Текущий шаг |
| | `next` (= draft) | Пунктир, приглушённые чернила, `ОЖИДАЕТ` | Предстоит |
| Определённость (Management_Model §3.3) | Туман войны | `.fog`: пунктирная зона «НЕТ ДАННЫХ» | «Надо доработать» — узел без KPI/владельца/данных; сигнал, не ошибка |
| | Бэклог | Визуального класса в прототипе пока нет | Осознанно отложено (см. «Открытые вопросы») |
| Перегруз ресурсов (PRD §41.1, `findOverloads()`) | Перегруз | `.ovl`: красный акцент-бордер + красноватый фон | Прямые этапы требуют юнита больше, чем выделено родителю |
| | Не выделен | `.ovl.warn`: жёлтый вариант | Юнит занят на этапах, но не выделен родителю вовсе |
| Проверки увязки (Management_Model §3) | ok / warn / error | `.check.ok` ✓ зелёный / `.wr` ⚠ жёлтый / `.er` ✕ красный | Находки советника: измеримость, связность, пробелы |
| Тон узла карты | done / warn / risk / next | `.g-done` зелёный / `.g-warn` жёлтый / `.g-risk` красный акцент-бордер; `.g-next` пунктир | Сводное здоровье цели на карте |
| Тип юнита | человек / AI | Круг `--ink` (`HumanDot`) / ромб `--op` (`AiDot`) | Единая рабочая сила (PRD §28): различие — маркер, не логика |
| Связи на карте целей | блокирует / выполнена / следующая | Красный бегущий пунктир / зелёная статичная / жёлтый бегущий пунктир (+ подпись 9px mono) | Отношения целей (Часть I §4) |

---

# D7. Движение

Два keyframe в `index.css` — весь моушен системы:

| Анимация | Параметры | Смысл | Где применяется |
|---|---|---|---|
| `os-pulse` | `2.4s ease-in-out infinite`, opacity 1 → 0.4 | «Живой» текущий элемент | `.pulse` (точки статуса активной цели), `.pm-pulse` (точка текущего узла карты процесса) |
| `os-flow` | `1.6s linear infinite`, `stroke-dashoffset: -40` | Поток по связям | `.canvas svg .flow` (связи карты целей), `.pm-edge.pm-now` / `.pm-ready` (активные рёбра карты процесса) |

**Правило доступности (обязательное):** `@media (prefers-reduced-motion: reduce) { * { animation: none !important } }` — при системной настройке «меньше движения» ВСЕ анимации выключаются. Уже в `index.css`; любые новые анимации обязаны гаситься этим же правилом.

---

# D8. Доступность

- **Фокус-обводки** — стандарт: `outline: 2px solid var(--gr); outline-offset: 1px` на `:focus-visible` для кнопок и инпутов обоих экранов и для SVG-узлов `.pm-node`.
- **Reduced motion** — см. D7.
- **Клавиатура и семантика**: интерактивные не-кнопки получают `role="button"` + `tabIndex={0}` + обработку Enter (`.wstage`, `.pm-node`); SVG карты процесса — `role="img"` с `aria-label`; иконочные кнопки — `aria-label` («Меньше загрузки», «Снять …», «Сообщение советнику»).
- **Контраст**: основной текст `--ink` на `--bg` — светлое на почти чёрном; иерархия вторичного текста строится прозрачностями `--i55/--i40/--i35` — самые тихие ступени применять только к неключевой информации.
- **Локализация**: весь UI-текст — на русском (CONTRIBUTING.md, раздел «Localization»); допустимы устоявшиеся техтермины и коды (`BI`, `KPI`, `GOAL-014`).

---

# D9. Использование бренд-бука

Как применять при разработке новых блоков UI:

1. **Токены** — только из `index.css :root`; новые цвета не заводить, оттенки — через rgba от существующих hex (до решения долга из D2).
2. **Компоненты** — сначала переиспользовать классы `os.css` (D5); новый класс — только когда ни одно семейство не подходит, в стиле существующих (короткие имена, вложенность под `.os-panel`/`.os-goal`).
3. **Семантика состояний** — строго по D6: зелёный = выполнено/ok, жёлтый = активно/внимание, красный = риск/дефицит, пунктир = черновик/будущее/нет данных. Не переназначать.
4. **Типографика** — mono для чисел/ID/меток, sans для контента; размеры из шкалы D3, новых не вводить.
5. **Движение** — только `os-pulse`/`os-flow`; любая анимация гасится reduced-motion (D7).
6. **Отклонения** — через ADR (`docs/adr/`), со ссылкой на пункт бренд-бука, от которого отклоняемся.

---

# D10. Дизайн-рендеры (источник)

Утверждённые рендеры, по которым построена визуальная система («per approved design renders» в шапке `index.css`), лежат в [`renders/`](renders/). Канон — пара `os-*`; серия `cc-*` — история итераций, сохранена как контекст решений.

| Рендер | Экран / состояние | Статус |
|---|---|---|
| [os-panel.png](renders/os-panel.png) | **Панель управления** (`.os-panel`, `CommandPanel.tsx`): карта целей с раскрытой GOAL-014, rail блоков слева, советники справа, нижний бар метрик | **Канон** — текущий прототип |
| [os-goal.png](renders/os-goal.png) | **Карточка цели** (`.os-goal`, `GoalCard.tsx`): GOAL-014, цепочка целей, ресурсы, этапы, чек-лист советника, блок безопасности | **Канон** — текущий прототип |
| [cc-root.png](renders/cc-root.png) | Итерация 1 «Вектор»: карта целей с круглыми узлами-датчиками, внешний/внутренний контуры, советники слева, панель цели и ресурсов справа | История |
| [cc-proc2.png](renders/cc-proc2.png), [cc-proc3.png](renders/cc-proc3.png) | Итерации карты процесса (этапы цели как BPMN-подобная цепочка в правой панели) | История |
| [cc-drill.png](renders/cc-drill.png) | Итерация проваливания в узел цели | История |
| [cc-restyle.png](renders/cc-restyle.png), [cc-restyle-drill.png](renders/cc-restyle-drill.png) | Рестайл серии `cc` в сторону Terminal Strategist (панель / дрилл) | История |
| [cc-final.png](renders/cc-final.png) | Финал серии `cc`: карта целей + этапы и карта процесса в правой панели, сигнал перегруза юнита | История — мост к `os-*` |
| [_archive/decision-center-list.png](renders/_archive/decision-center-list.png) | Устаревший CRUD-экран списка решений (временный каркас до игровой панели) | Архив |

Правило: новые утверждённые рендеры складывать в `renders/` и дописывать в эту таблицу; устаревшие — переносить в `renders/_archive/`, не удалять.

---

# Открытые вопросы (Часть II)

Зафиксировано, не решать догадкой и не чинить без отдельной задачи:

1. **Именование продукта — частично решено (2026-07-07, см. D1):** продукт/репо — ОС `ai-agents`, «Decision Center» — имя первого среза. Открытым остаётся финальное потребительское имя: «Вектор·OS» — кандидат, решение за владельцем; до решения в UI-коде остаётся «Вектор·OS».
2. **Долг хардкод-цветов** (D2): `#161618`, `#101012`, `#1e2117`, `#181a16`, `#131510`, дубли `#141416`/`#0a0a0b`, rgba-литералы и hex в TSX (`CommandPanel.tsx`, `GoalCard.tsx`, `ProcessMap.tsx`). Кандидаты в токены (например, `--sf0` для `#101012`, `--gr-bg` для `#1e2117`), но НЕ править в этом проходе.
3. **`.ovl.crit` без правила.** `GoalCard.tsx` вешает класс `crit` на критический перегруз, но в `os.css` правила `.ovl.crit` нет — визуально работает базовый красный `.ovl`. Либо добавить правило, либо убрать класс.
4. **Захардкоженные проценты в CSS/разметке**: ширина бара раскрытой цели (`.gx > .bar i` — `width: 58%`) и трека здоровья (`.bot .hp .tr i` — `width: 68%`) заданы в CSS и совпадают с демо-данными случайно, а не по данным; подпись `6 ЦЕЛЕЙ, 5 СВЯЗЕЙ` статична.
5. **Частично мёртвые демо-данные.** `GOAL_CARD` в `data.ts`: карточка цели использует из него только `visibility`/`security`/`grants`; статические `chain`/`stages`/`kpis`/`advisorChecks` вытеснены рекурсивным `GOAL_TREE` + динамическим чек-листом и не рендерятся.
6. **Визуальный код «бэклога»** (Management_Model §3.3) в прототипе не реализован — есть только «туман войны» (`.fog`). Понадобится при реализации состояний определённости.
7. **Шрифты с CDN.** IBM Plex подключается с Google Fonts (`index.html`); офлайн-фоллбек — системные Segoe UI / Cascadia Mono / Consolas. Селф-хостинг — отдельное решение.
