# DEVLOG — часть 05

Записи с 2026-07-15 по 2026-07-16.

## 2026-07-16 · fix/frontend-goal-popup-style · не закоммичено

- **Что сделано:**
  - Промпт №36 (фикс-ап Ф7a) выполнен по Scope: попап карточки цели (`GoalPopup.tsx`) переведён с монолитной рамки (`.gpop-card`, `var(--sf)` + бордер `var(--ln)` вокруг всего содержимого) на колонку свободных «облачков» (`.gpop-col`/`.gpop-bub`) той же породы, что `.omsg`/`.topic` оверлея разговора — затемнение и появление переиспользуют `.ov-bg`/`os-ov-in` как есть, без изменений.
  - Рамки облачков — единая схема: цвет идентичности ветки цели (`branch.bd` из `branchStyle()`), а не нейтральный `--ln`; нейтральный `--ln` оставлен только для внутренней строки KPI (`.gpop-bub .rc` — фон/бордер сняты, чтобы не давать рамку-в-рамке). Единый gap (9px) между облачками; хвост «Esc / клик мимо — закрыть» сидит сразу под последним облачком, колонка высотой по контенту — пустого хвоста под хинтом больше нет.
  - Правило кодифицировано в бренд-буке: новый раздел `Visual_Reference.md` D9 «Оверлеи» («один паттерн оверлея на весь продукт» — затемнение/появление/без монолитных панелей/рамки-идентичность/светофор-пунктир не переназначаются/единый ритм/выходы), старые D9/D10 сдвинуты на D10/D11; поправлена внутренняя перекрёстная ссылка `(D10)` → `(D11)` и указатель в `.claude/rules/frontend.md`.
  - Playwright-проход: попап открывается/закрывается (Esc, ✕, клик по фону), спот-чек мутаций (пауза/возврат в активную, инлайн-правка имени — открытие поля подтверждено) прошёл без регрессий; визуально сверено с оверлеем разговора — то же затемнение, та же порода облачков, нет сплошной рамки. Скриншот — `renders/f7a-popup-style.png`.
  - `frontend`: `npm run build`/`npm run lint` — зелёные. `backend` (контроль): pytest (103 passed) — зелёный, ничего не менялось.
- **Дальше:**
  - Слайс B (edge-decomposition на карте) и бэкенд-поля `deadline`/`importance`/`urgency` — как и раньше, отдельные будущие срезы (см. `BACKLOG.md`).
  - Миграция других поверхностей (попап-оверы `GoalCanvas`, будущие оверлеи) на кодифицированный паттерн D9 — добавлено в `BACKLOG.md`.
  - Мерж — через субагента `merger`.

## 2026-07-15 · feat/frontend-goal-popup · не закоммичено

- **Что сделано:**
  - Промпт №35 (финал, Шаг Ф7a) выполнен по Scope: карточка цели теперь попап поверх карты (`frontend/src/os/GoalPopup.tsx`, новый компонент, только для реальных целей), страница `RealGoalCard.tsx` удалена. Дерево целей рендерится в одном месте — карта в `CommandPanel.tsx`.
  - Оверлей — стиль advisor-orb (`.ov`/`.ov-bg`/`.ov-orb`/`.ov-name`), орб слева — крупный hex в цвете идентичности ветки узла (`branchStyle()`), карточка справа — новый класс `.gpop`. Шапка: подпись «КАРТОЧКА ЦЕЛИ», бейджи определённости/риска (риск — только на чтение), безрамочный ряд иконок-глифов (активна/пауза/процесс-заглушка/канвас/удалить/✕) с `title`+`aria-label`, без текстовых подписей.
  - Инлайн-правка имени/описания/владельца и KPI — тот же F3-паттерн и diff-sync по id, что были в `RealGoalCard.tsx` (скопировано до удаления файла); мутации идут через `saveField()` (PATCH → reload), после успеха перезапрашиваются и цель, и `listGoals()` — карта обновляется без смены роута.
  - «СРОК» и «ВАЖНОСТЬ · СРОЧНОСТЬ» — dashed-заглушки без бэкенд-полей и без слов SMART/Эйзенхауэр (только tooltips). Декомпозиция («+ подцель», «изменить родителя») из попапа убрана вместе со страницей — ждёт слайса B (edge-affordances на карте).
  - `App.tsx`/`CommandPanel.tsx` перевязаны: канвас постановки (`GoalCanvas.tsx`) — единственный оставшийся полноэкранный переход для реальных целей, открывается из иконки попапа; демо-флоу (`GoalCard.tsx`/`data.ts`) не тронут.
  - CSS: общие ряды/бейджи/инпуты (`.rrow`, `.radd`, `.rdel`, `.edit`, `.bdg`, `.cap`, `.rc`/`.rc2`, `.note`, фокус-outline) расширены запятой на `.gpop` без дублирования блоков; новые правила — только контейнер/шапка/иконки/квадранты (`os.css`, в конце файла).
  - Playwright-проход по DoD: клик по узлу открывает попап без смены роута; инлайн-правка описания сохраняется и видна сразу; пауза переводит цель в бэклог (узел пропадает с карты, счётчик обновляется); канвас постановки открывается и «← карточка» возвращает на карту; Esc закрывает попап; удаление (с `window.confirm`) убирает цель, закрывает попап, обновляет карту. Скриншот — `renders/f7a-goal-popup.png` (цель «Внедрение CRM», KPI + непустой владелец).
  - `frontend`: `npm run build`/`npm run lint` — зелёные. `backend` (контроль): pytest (103 passed)/ruff check/ruff format --check/mypy — зелёные, ничего не менялось.
  - Хвост Ф6 (Playwright-проход промпта №32, отложенный в прошлой записи) закрыт как неактуальный — экран, который он должен был проверять (`RealGoalCard.tsx`), в этом же проходе удалён.
- **Дальше:**
  - Слайс B: edge-«+» decomposition на карте (восстанавливает создание подцели/смену родителя, которые ушли вместе со страницей) — отдельный промпт после подтверждения owner'а.
  - Бэкенд-слайс: поля `deadline`/`importance`/`urgency` (сначала ADR + миграция), затем можно оживить «СРОК» и квадранты 2×2.
  - «Назначить процесс» — отдельный слайс, нужен свой ADR.
  - Юнит-сущность для назначения владельца цели (сейчас свободный текст).
  - CSS-уборка осиротевших `.os-goal`-правил страницы (см. `BACKLOG.md`).
  - Мерж — через субагента `merger`.

## 2026-07-15 · chore/kickoff-recommendation-fixup

- **Что сделано:** промпт №34 выполнен по Scope — фикс-ап намерения промпта №33. На практике 2026-07-15 подтвердилось: Cowork сгенерировал handoff для Ф7а без рекомендации модели/усилия, формально следуя букве промпта №33 (правило было привязано только к kickoff-сообщению). Buллет «Kickoff carries a model + effort recommendation» в `COWORK.md` («Working with the `prompts/` folder») заменён на «Model + effort recommendation at both milestone-opening points»: рекомендация теперь фигурирует и в (a) handoff-блоке, открывающем новую сессию для вехи, и в (b) kickoff-сообщении для Claude Code — повтор намеренный (owner видит рекомендацию и при старте сессии, и при запуске прохода). Дефолт остаётся Sonnet/medium, гардрейлы `.claude/settings.json` не тронуты. Остальная часть секции не изменена.
- **Дальше:** ничего специально не запланировано; при следующей открываемой вехе проверить, что рекомендация действительно появляется в обеих точках (handoff и kickoff).

## 2026-07-15 · chore/kickoff-model-recommendation

- **Что сделано:** промпт №33 выполнен по Scope. В `COWORK.md` («Working with the `prompts/` folder») добавлен bullet сразу после строки про kickoff-line: kickoff-сообщение Cowork при открытии новой вехи теперь несёт «Рекомендуемая модель: X, усилие: Y» с однострочным обоснованием; дефолт остаётся Sonnet/medium, гардрейлы `.claude/settings.json` не трогаются, модель выше Sonnet легализуется только подтверждением owner'а такой рекомендации (это и есть «явная инструкция» из CLAUDE.md «Token economy»). Остальная часть секции не тронута.
- **Дальше:** ничего специально не запланировано; следовать новому формату kickoff-сообщений при открытии следующих вех.

## 2026-07-15 · feat/frontend-goal-decomposition · не закоммичено

- **Что сделано:**
  - Промпт №32 (Шаг Ф6) выполнен по Scope: карточка цели (`RealGoalCard.tsx`) разделена на секцию «КОНТЕКСТ» (grandparent + parent, ровно 2 уровня вверх — owner-решение 2026-07-15, не вся цепочка до корня) и секцию «ДЕКОМПОЗИЦИЯ» (прямые подцели + «+ подцель»); честная пустая строка «подцелей нет — добавьте первую» вместо прежней комбинированной.
  - «+ подцель» в карточке — F3-паттерн (инлайн `input` с `aria-label`, Enter → commit, Escape/пустое имя → cancel без запроса) → `createGoal({ name, parent_id })` → полный `reload()`.
  - «+ подцель» на канвасе постановки (`GoalCanvas.tsx`) — синтетический узел-черновик в секторе подцелей, визуально dashed (`.gc-sub-draft`, новый класс в `os.css`), клик открывает `gc-popover` с тем же именем/commit/cancel; Escape сначала закрывает черновик подцели, затем идёт по существующей цепочке (фактор → связь → выбор действия → назад).
  - Секция «РОДИТЕЛЬ И РАЗМЕЩЕНИЕ» (родитель-пикер + бэклог-переключатель) не тронута — остаётся инструментом редактирования, отдельно от читающего «КОНТЕКСТА».
  - `frontend`: `npm run build`/`npm run lint` — зелёные; `backend` (контроль): pytest/ruff/ruff format/mypy — зелёные, ничего не менялось.
  - Playwright MCP не подключился в этой сессии (сервер не появился после ожидания) — вместо браузерного прохода создание подцели проверено напрямую через API (`POST /goals` с `parent_id`, сид БД с целью на глубине 2 для проверки «ровно 2 уровня» и корневой целью для проверки «нет строк контекста»); UI-флоу визуально не проверен в браузере.
- **Дальше:**
  - Владельцу — при случае прогнать Playwright-проход по DoD этого промпта вручную (или в сессии, где MCP `playwright` подключается) — этот проход его не покрыл.
  - Две новые строки в `BACKLOG.md` (бизнес-процесс исполнения цели; перекрытие узлов на плотных картах) — без изменений, ждут своих будущих срезов.
  - Мерж — через субагента `merger`.

## 2026-07-15 · chore/hygiene-closed-milestone · не закоммичено

- **Что сделано:**
  - Промпт №31 выполнен по Scope, один `chore:` PR: триггер `/handoff` из промпта №28 сработал корректно на моменте «веха закрыта», но после предложения хэндоффа владелец сменил тему (новые продуктовые вопросы), и Cowork начал новую работу внутри сессии закрытой вехи вместо повторного предложения выхода. Триггер «context drifted» существовал как формулировка, но не был привязан к запрету на новую работу — пробел закрыт.
  - В `COWORK.md` (раздел «Session hygiene (context economy)»), сразу после буллета про триггер `/handoff` (промпт №28), добавлен новый буллет: после «веха закрыта» любой новый запрос вне хвостов закрытой вехи получает тот же ответ — повторное предложение `/handoff` + новая сессия, без начала новой работы здесь; новые продуктовые вопросы владельца явно названы типичным вектором дрейфа контекста, а не хвостом.
  - `prompts/prompt-31-hygiene-closed-milestone.md` закоммичен прямо в `prompts/_done/` в этом же PR.
  - `backend/`/`frontend`/тесты/`.claude/settings.json`/`docs/full-vision` не тронуты.
- **Дальше:**
  - Мерж — через субагента `merger`.

## 2026-07-15 · feat/frontend-kpi-factor-editing · не закоммичено

- **Что сделано:** Шаг Ф5 (промпт №30) — управление факторами композитного KPI с канваса постановки. `api.ts`/`types.ts`: `KpiFactorCreate`, `createKpiFactor`, `deleteKpiFactor` (backend не менялся, роуты были с Шага 3b). `GoalCanvas.tsx`: клик по собственному KPI теперь открывает выбор действия («связать KPI…» / «добавить фактор…»); флоу добавления фактора зеркалит флоу связи (цель → KPI с числовым таргетом → вес) и создаёт `POST /kpi-factors`; удаление — по dashed-рёбрам факторов (hit-path `role="button"`/Enter, как у связей) через `DELETE /kpi-factors/{id}` с `window.confirm`; ошибки 409/400 — русским текстом («Такой фактор уже добавлен» / «Нельзя добавить фактор…»); `Escape` закрывает поповеры по цепочке фактор → связь → выбор действия → назад.
- Playwright-проходом на сидах подтверждено: добавление фактора (вес 0.4) даёт dashed-ребро «×0.4» и расчётное значение композита, повторное добавление даёт 409-текст, удаление возвращает KPI к плановому таргету, флоу связи не задет.
- Backend-контроль (`pytest`/`ruff`/`ruff format`/`mypy`) и frontend (`npm run build`/`npm run lint`) — зелёные, без изменений на бэкенде, как и предписано скоупом.
- **Дальше:** закоммитить, поднять PR в `main`, перенести `prompts/prompt-30-kpi-factor-editing.md` в `prompts/_done/` с обновлением `prompts/README.md`. Возможный будущий срез — PATCH веса существующего фактора (сейчас удалить+добавить), отмечен в BACKLOG, если удалить+добавить окажется неудобным на практике.

## 2026-07-15 · chore/token-guardrails · не закоммичено

- **Что сделано:**
  - Промпт №29 выполнен по Scope, один `chore:` PR: причина, по которой владелец сжигал 5-часовой лимит примерно за час — extended thinking включён, effort High, модель не закреплена (UI-дефолт может дрейфовать), субагент `merger` на `inherit`.
  - В `.claude/settings.json` добавлены `model: claude-sonnet-5`, `alwaysThinkingEnabled: false`, `effortLevel: medium`, а в `env` — `MAX_THINKING_TOKENS: "0"` (жёстко отключает thinking на уровне Anthropic API вне зависимости от UI-тумблеров).
  - `.claude/agents/merger.md`: `model: inherit` → `model: haiku` — git-механика не требует Sonnet.
  - Новый хук `.claude/hooks/session-budget.sh`, зарегистрирован в `settings.json` под `hooks.UserPromptSubmit`: если транскрипт сессии превышает 1.5 MB, отдаёт предупреждение владельцу через `additionalContext`; иначе — тихий `exit 0`. Оба пути (ниже/выше порога) проверены вручную.
  - `CLAUDE.md` → «Token economy»: добавлена строка про то, что гардрейлы закреплены в `.claude/settings.json` и их нельзя ослаблять без прямого указания владельца.
  - `backend/`/`frontend`/graphify/`protect-main.sh`/`block-secrets.sh`/`format-code.sh` не тронуты.
- **Дальше:**
  - Мерж — через субагента `merger` (теперь уже на `haiku`).
  - Прикладной прогон: понаблюдать в следующих сессиях, действительно ли лимит перестал сжигаться за ~1 час.

## 2026-07-15 · chore/handoff-trigger-hook · не закоммичено

- **Что сделано:**
  - Промпт №28 выполнен по Scope, один `chore:` PR: правило гигиены сессий («веха закрыта / смена контекста → `/handoff` → новая сессия») было плавающим принципом, ни к какому конкретному шагу не привязанным — из-за этого его пропускали (что и произошло на практике). Привязано к точному моменту: пост-проходной верификации Cowork через GitHub MCP, ровно к моменту, когда Cowork готова написать владельцу «веха закрыта».
  - В `COWORK.md` (раздел «Session hygiene (context economy)») добавлен буллет: то же сообщение, в котором Cowork подтверждает «веха закрыта», обязано сразу предложить `/handoff` + новую сессию — без предложения продолжить новую работу в сессии закрытой вехи.
  - `backend/`/`frontend`/тесты/branch protection не тронуты.
- **Дальше:**
  - Мерж — через субагента `merger` (dogfood; `merger.md` в этом проходе не редактируется, безопасно).

## 2026-07-15 · chore/i18n-ops-english · не закоммичено

- **Что сделано:**
  - Промпт №27 выполнен по Scope, один `chore:` PR: операционные/агентские файлы переведены на английский (экономия токенов — RU стоит ~2× токенов при постоянном чтении) — корневой `CLAUDE.md`, `COWORK.md`, все `.claude/rules/*.md` (backend/frontend/docs/commits), все `.claude/agents/*.md` (code-reviewer/spec-guardian/doc-keeper/test-runner/merger), все `.claude/commands/*.md` (adr/devlog/ship/handoff). `.claude/CLAUDE.md` уже был на английском — не тронут.
  - Сохранены по-русски: цитаты продуктового канона и UI-примеры; выводимые командами шаблоны — блок «Перенос контекста» (`handoff.md`), формат записи DEVLOG (`devlog.md`), структура/статус ADR (`adr.md`); литеральное правило «Язык файлов:» в `COWORK.md` (новая формулировка языкового правила, вставлена как есть). `docs/**`, `README.md`, `CONTRIBUTING.md` не тронуты.
  - Кодифицировано правило языка файлов в `COWORK.md` («Пуш и ответственность (кратко)») + кросс-ссылка в `commits.md`.
  - Устранена утечка «хвостового» PR: раньше промпт-файл коммитился в `prompts/`, а после мержа отдельным шагом переносился в `prompts/_done/` (провоцируя второй PR). Теперь и `COWORK.md` («Хвост-уборка», «Работа с папкой `prompts/`»), и `.claude/agents/merger.md` (шаг «Стейджинг») требуют коммитить промпт-файл сразу в `prompts/_done/` в том же PR. Этот проход это же и демонстрирует: `prompts/prompt-27-i18n-ops-english.md` закоммичен прямо в `prompts/_done/`.
  - **spec-guardian** прогнан по полному диффу перевода — сдвига смысла не найдено (проверены `commits.md`, `protect-main`-ссылки, полная последовательность шагов `merger.md`, консистентность нового правила «хвост-уборки» между `COWORK.md` и `merger.md`, сохранность RU-шаблонов в `handoff.md`/`devlog.md`/`adr.md`).
  - `backend/`/`frontend`/тесты/GitHub branch protection не тронуты.
- **Дальше:**
  - Мерж этого PR — обычным `gh pr merge --merge --delete-branch` (не через `merger`-агента: сам `merger.md` в этом проходе редактируется).
