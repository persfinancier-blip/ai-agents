# DEVLOG — часть 07

Записи с 2026-07-16 по 2026-07-17.

## 2026-07-17 · main · 665e88c (промпт №45, PR #50)

- **Что сделано:**
  - Слайс 45 — пикер владельца цели в `GoalPopup.tsx`: строка «отв. …» стала кликабельной, зеркалит паттерн `ParentPicker` (слайс 37). Новый `OwnerPicker` — облачко D9 с `<input>`-поиском (фильтр по имени, case-insensitive substring) над секциями «Юниты»/«Департаменты»/«Команды» (только непустые, `unitKindLabel`/`groupKindLabel` + D10-цвет для юнитов), «— не назначен» первым пунктом снимает owner.
  - Типы/клиент: `UnitGroupKind`/`UnitGroupRead` в `types.ts` (зеркало `backend/app/schemas/unit_group.py`), `listUnitGroups()` в `api.ts`, `GROUP_KIND_LABEL`/`groupKindLabel` в `os/units.ts`. Выбор в пикере → `patchGoal(goalId, { unit_id })` (юнит или группа, ADR-0007) → `reload()`+`onChanged()`, как у остальных полей попапа.
  - Backend не менялся — контракт (`PATCH /goals/{id}` принимает unit- или group-`entity_id`, 404/422 на невалидный id) уже был готов промптом №44.
  - Проверки: `npm run build`/`npm run lint` — зелёные. Playwright MCP не подключился в этой worker-сессии — вместо него прогнан headless-скрипт (временный `playwright`, не закоммичен) на живом дев-сервере (backend `uvicorn` + сид `seed_demo_goals.py` + два `unit-group` через API): открытие попапа цели «Внедрение CRM» → клик по «отв.» открывает пикер → поиск «команда» фильтрует до раздела «Команды» → выбор группы «Команда роста» обновляет owner на её имя; повторное открытие + поиск «Соколова» → выбор юнита обновляет owner; «— не назначен» возвращает «—». Скриншот — `renders/slice45-owner-picker.png`.
- **Дальше:**
  - Следующий слайс — дерево департаментов + команды в панели юнитов (`UnitsPanel.tsx`).
  - Владельцу — при случае прогнать Playwright-проход этого промпта в сессии, где MCP `playwright` подключается (этот проход его не покрыл, использован скриптовый `playwright` напрямую).

## 2026-07-17 · main · 6609044 (промпт №44, PR #49)

- **Что сделано:**
  - ADR-0007 (группировка юнитов, backend-only): `unit_group` как Entity-подтип (`kind ∈ {department, team}`), `unit_group.parent_id` — строгое дерево департаментов (один родитель, запрет циклов), `unit.department_id` — один дом на юнит, `team_membership` — m2m-оверлей для команд (плоские в этом слайсе).
  - `goal.unit_id` FK перенаправлен с `unit.entity_id` на `entity.id` (миграция `064d0e836915`, `batch_alter_table` — прецедент из `0f9898942202`): цель теперь может принадлежать юниту ИЛИ группе, сервисный слой валидирует тип сущности (422 для прочих). `compute_definiteness` не менялся, как требовал скоуп.
  - API `/api/v1/unit-groups` (CRUD + `POST`/`DELETE`/`GET` `.../members/{unit_id}`); `/api/v1/units` получил `department_id` в create/patch/read. Удаление группы нулит входящие ссылки (`goal.unit_id`, `unit.department_id`, дочерние `parent_id`, `team_membership`).
  - `alembic upgrade head` на чистой БД и цикл upgrade → downgrade → upgrade проверены вручную. `ruff check`/`ruff format --check`/`mypy app`/`pytest` (135 тестов, было 122) — зелёные.
  - Канон: `Management_Model.md` §2/§6/§8 (типы юнита) обновлены со ссылкой на ADR-0007; ADR-0006 «Открытые вопросы» п.2 помечен решённым; `docs/adr/README.md` + `BACKLOG.md` обновлены.
- **Дальше:**
  - Следующий слайс — фронтенд: пикер юнит/группа с поиском в `GoalPopup`, дерево департаментов + команды в панели юнитов (`UnitsPanel.tsx`).
  - Открытые вопросы ADR-0007 не решались этим проходом: агрегация ресурсов/загрузки группы, вложенность команд, права на изменение состава, переименование `unit_id → owner_id`.

## 2026-07-17 · main · f464e70 (промпт №43, PR #48)

- **Что сделано:**
  - Панель юнитов (read-only) поверх карты целей: `UnitRead`/`UnitKind` в `types.ts` (зеркало `backend/app/schemas/unit.py`), `listUnits()` в `api.ts`, RU-подписи вида юнита (`os/units.ts` — «Сотрудник/Агент/Вне контура/Устройство», цвет по D10 через существующие `--id-*` токены, без новых). `UnitsPanel.tsx` — новый оверлей, переиспользует `.ov`/`.ov-bg`/`.gpop-col`/`.gpop-bub`/`.gpop-hd`/`.gpop-ic` (тот же паттерн, что советник и карточка цели), три состояния (загрузка/пусто «Юнитов пока нет»/ошибка). Открывается кнопкой «ЮНИТЫ» в хедере `CommandPanel.tsx`, не задевает карту целей и демо-данные (`data.ts`/`goals.ts`/`goalTree.ts` не тронуты).
  - Playwright MCP недоступен в этой (воркерной) сессии — self-verification прогнан вручную через `playwright` (Python) на живом dev-сервере (`npm run dev` + backend на sqlite, `LLM_PROVIDER=stub`): 4 сидированных юнита (по одному на вид) видны с верными RU-подписями, затем БД очищена (`DELETE /api/v1/units/{id}` по всем) и подтверждён пустой стейт «Юнитов пока нет», затем юниты пересозданы и снят финальный скриншот `renders/slice43-units-panel.png`. `npm run build`/`npm run lint` — зелёные; backend не менялся (контроль не гонялся).
- **Дальше:**
  - Следующий слайс — назначение юнита из попапа цели (пикер `unit_id` в `GoalPopup`, `patchGoal({unit_id})`) — этим проходом не начинался.
  - Владельцу — при желании прогнать Playwright MCP-проход вручную в сессии, где сервер подключается (этот проход его не покрыл, использован скриптовый `playwright` напрямую).

## 2026-07-17 · main · 200f0a0 (промпт #41b, PR #28)

- **Что сделано:**
  - Локальный проход вместо авто-диспатча (промпт `prompt-42-unit-entity-backend` дважды падал на воркере — лимиты/сбой, issues #37/#40): ADR-0006 backend-срез (Unit-подтип Entity, `goal.unit_id`, одноразовая миграция owner→Unit, `compute_definiteness` на `unit_id`, Goal API без `owner`, read-only `unit_name` во фронтенде) уже был реализован на этой ветке поверх старого мерж-бейза (`71d4eff`) — вместо повторной реализации ветка перебазирована на текущий `main` (`9838292`); конфликт только в `docs/DEVLOG.md` (наложение записей), разрешён вручную.
  - DoD подтверждён локально после ребейза: backend `ruff check`/`ruff format --check`/`mypy app`/`pytest` (122 теста) и frontend `npm run build`/`npm run lint` — зелёные; миграция `0f9898942202` вручную прогнана на копии сидированной БД с pre-migration owner-текстом (два разных `owner` → два разных `Unit(kind=employee)`, дедуп по тексту, `goal.unit_id` проставлен верно).
  - Housekeeping: заголовки записей №39 и №40 в DEVLOG исправлены (№39 — отсутствовал целиком, слился с телом №40; №40 — «не закоммичено» вместо реального состояния) на `main · <sha> (промпт №NN, PR #N)`. Опечатка «прompt #41» в BACKLOG.md не найдена в текущем файле (видимо, уже исправлена ранее).
- **Дальше:**
  - Открыть PR (заменяет собой PR #28 — старый закрывается с пометкой), закрыть issues #37/#40, удалить ветки неудачных воркер-прогонов `task/prompt-42-unit-entity-backend-20260717100116`/`-20260717142736`.
  - После мержа — финализировать заголовок этой записи (`промпт #41b`) на `main · <merge-sha> (PR #N)`, тем же способом.

## 2026-07-17 · main · 9838292 (промпт ops-07, PR #39)

- **Что сделано:**
  - Написан `docs/PROCESS.md` — протокол всего цикла разработки (роли, жизненный цикл задачи, запасные пути, формат промпта, защита от сбоев, память/состояние, инварианты, мини-FSM состояний задачи), собран из `COWORK.md`, `CLAUDE.md`, `.claude/rules/*`, `prompts/README.md` — только описание существующего, без изменения самого процесса. Три пункта разногласий между источниками вынесены в «Открытые вопросы» вместо тихого исправления.
  - `README.md` («Правила разработки») и `CLAUDE.md` («Process») — по одной ссылающейся строке на `docs/PROCESS.md`.
  - Промпт `prompts/prompt-ops-07-process-canon.md` перенесён в `prompts/_done/`.
- **Дальше:**
  - Открытые вопросы из PROCESS.md (нужен ли `.claude/rules/process.md`, опечатка структуры в `prompts/README.md`) — на усмотрение владельца, не решались этим проходом.

## 2026-07-17 · main · cee66a0 (промпт ops-06, PR #38)

- **Что сделано:**
  - `COWORK.md`, раздел «Working with the `prompts/` folder» — старая формулировка «Don't duplicate the whole prompt text in chat» дополнена явным правилом: тела промптов никогда не появляются в чате с владельцем (ни при диспатче, ни в отчётах, ни «для справки»); диспатч — одна строка (имя промпта + суть в двух словах), отчёты — только выводы и действия, без содержимого файлов/диффов/цитат промпта.
  - `.claude/rules/github-automation.md` — добавлена та же однострочная норма в раздел file-driven dispatch.
  - Промпт `prompts/prompt-ops-06-no-prompt-echo.md` перенесён в `prompts/_done/`.
- **Дальше:**
  - Это первый живой end-to-end прогон полного пайплайна включая auto-merge — если гейт зелёный, PR мержится сам; если нет — PR остаётся открытым с комментарием о блокере.

## 2026-07-17 · main · fd07ad4 (промпт ops-05, PR #36)

- **Что сделано:**
  - `claude-task-push` в `.github/workflows/claude.yml` — добавлен шаг «DoD gate» (голый shell, без Claude): по решению владельца от 2026-07-17 воркерский PR с зелёным гейтом мержится сам (`gh pr merge --merge --delete-branch`), без нажатия кнопки владельцем. Гейт вычисляется по зонам из `git diff --name-only origin/main...HEAD` (backend → ruff/mypy/pytest, frontend → lint/build, только docs/prompts → проходит тривиально) прямо внутри job'а, потому что PR/мержи от `GITHUB_TOKEN` не будят другие workflow (защита GitHub от рекурсии) — `ci.yml` на воркерских PR и на `main` после бот-мержа не запускается.
  - Красный гейт → PR остаётся открытым с комментарием `🔴 Проверки не прошли: <что>`; блокировка настройками репозитория (если бот не может мержить) — логируется явно, без самовольного изменения настроек.
  - `.claude/rules/github-automation.md` — зафиксировано решение об auto-merge (Cowork-верификация переезжает post-merge, откат = revert PR) и отдельное правило: правки `.github/workflows/**` — только локальным проходом (`GITHUB_TOKEN` не имеет прав `workflows`, подтверждено на run 29559783511).
  - `COWORK.md` — то же решение отражено в разделе «Push and responsibility».
- **Дальше:**
  - Этот PR — исключение из решения (сам меняет процесс): остаётся открытым для последнего ручного мержа владельца; после него auto-merge активируется для всех последующих воркерских PR.
  - Владельцу: после мержа проверить первый реальный воркерский прогон end-to-end (зелёный гейт → авто-мерж без вмешательства).

## 2026-07-17 · chore/fix-push-bridge · не закоммичено (промпт ops-04)

- **Что сделано:**
  - Починен путь `task/**` push → воркер, найденный сломанным в ops-03: `anthropics/claude-code-action@v1` не поддерживает событие `push` («Unsupported event type: push»). Проверено, что `workflow_dispatch`/`repository_dispatch`-мост тоже не работает — `GITHUB_TOKEN` по умолчанию не может разбудить второй workflow (защита GitHub от рекурсии); PAT/App-токен не заводили без расширения секретов, поэтому выбран вариант из промпта #3.
  - `claude-task-push` в `.github/workflows/claude.yml` теперь ставит `@anthropic-ai/claude-code` и гоняет `claude -p` напрямую (`CLAUDE_CODE_OAUTH_TOKEN`), а не через action.
  - Живым тестированием (5 итераций) вскрыты и исправлены ещё два реальных препятствия: (1) CLI на голом раннере не проходит startup-диалог доверия к workspace — исправлено предварительной записью `hasTrustDialogAccepted` в `~/.claude.json` перед вызовом; (2) `.git` — защищённая директория в Claude Code, `git push` там всегда ждёт подтверждения, даже под `--dangerously-skip-permissions` (по дизайну CLI, не баг) — решение: Claude только коммитит локально, `git push` + `gh pr create` вынесены в отдельный шаг workflow (голый shell, вне сессии Claude).
  - По итогам первого чистого прогона всплыла настройка репозитория `Actions → General → can_approve_pull_request_reviews` (была `false` по умолчанию) — блокировала `gh pr create` от `GITHUB_TOKEN`; включена (`gh api .../actions/permissions/workflow`, с подтверждения владельца).
  - Финальный прогон (PR #33, ветка `task/smoke-test-ops04-...`) прошёл end-to-end без вмешательства: push → воркер → коммит → push → PR. Артефакты подчищены: ветки/worktree удалены, ложный issue `worker-failure` (#31 — из отладочного прогона до фикса .git) закрыт с комментарием.
  - `.claude/rules/github-automation.md` — описан рабочий мост (CLI напрямую, не action).
- **Дальше:**
  - Владельцу: смёржить `chore/fix-push-bridge`, затем `schtasks /Run /TN "ai-agents-task-dispatch"` чтобы запустить вотчер (уже ретаргетирован в ops-03) — и путь prompt-файл → `task/*` → PR становится живым для реальных промптов.

## 2026-07-16 · chore/standalone-dispatcher · не закоммичено (промпт ops-03)

- **Что сделано:**
  - `scripts/dispatch-tasks.ps1` — параметр `-RepoRoot <path>` (по умолчанию — родитель каталога скрипта, поведение in-repo не меняется). Лог (`dispatch.log`) теперь пишется рядом с САМИМ скриптом (`$PSScriptRoot`), а не внутри `$RepoRoot` — иначе он не работал бы, когда репозиторий стоит на ветке без `scripts/`.
  - Установленная копия скрипта — `%LOCALAPPDATA%\ai-agents-ops\dispatch-tasks.ps1`; задача Task Scheduler `ai-agents-task-dispatch` (уже была зарегистрирована владельцем вручную) требует ретаргетинга на неё, но `schtasks /Change` отказал в доступе (нужны админ-права) — точная команда для владельца ниже.
  - Наблюдатель запущен вручную из установленной копии (`-RepoRoot C:\Dev\ai-agents`), проверено: процесс жив, `dispatch.log` создаётся рядом с установленной копией (не в репозитории).
  - **Побочная находка:** при проверке наблюдатель реально задиспатчил сам файл этого промпта (`prompt-ops-03-...md`, он был untracked в рабочем дереве владельца) в ветку `task/*` — это вскрыло реальный баг: `claude-code-action@v1` не поддерживает событие `push` («Unsupported event type: push»), то есть весь push-триггер из PR #27 сейчас нерабочий на `main`. Ветка `task/*` удалена, ложное issue `worker-failure` (#29) закрыто с комментарием, файл промпта возвращён в `prompts/` рабочего дерева владельца в исходное состояние. Баг `claude.yml` — вне рамок ops-03 (правки `claude.yml` не в Scope), не чинил.
  - `.claude/rules/github-automation.md` — описаны установленная копия, `-RepoRoot`, правило «после правки скрипта — обязательно перекопировать в install dir».
- **Дальше:**
  - **Баг push-триггера в `claude.yml`** (`claude-code-action@v1` не понимает `push`) — нужен отдельный проход/промпт, иначе диспетчер реально ничего не доставляет воркеру.
  - Владелец выполняет команду ретаргетинга Task Scheduler в admin PowerShell (см. отчёт).
  - После починки push-триггера — не запускать наблюдателя на реальном `prompts/`, пока баг не исправлен (иначе каждый новый промпт-файл будет уходить в проваленный run).

## 2026-07-16 · chore/file-task-dispatch · не закоммичено (промпт ops-02)

- **Что сделано:**
  - `.github/workflows/claude.yml` — добавлен триггер `push` на ветки `task/**`; исправлен дефект скоупинга промпта (найден при ревью PR #26): раньше один `prompt` уходил на все события, теперь три отдельных job'а (`claude-issue`, `claude-task-push`, `claude-comment`) — comment-события идут без `prompt` (tag-режим action). Добавлены `timeout-minutes: 30` на все job'ы и token-free шаг-«будильник» (`actions/github-script`, без токенов Claude) на `failure() || cancelled()`: issue-путь — комментарий на issue, task-push-путь — новый issue с меткой `worker-failure` и ссылкой на run. Метка `worker-failure` создана в репозитории (не триггерит воркер — проверено по условиям `if`).
  - `scripts/dispatch-tasks.ps1` — диспетчер: `-Once` (разовый прогон) и режим наблюдателя по умолчанию (`FileSystemWatcher` на `prompts/`, без поллинга, дебаунс 60с). Для каждого нового файла `prompts/prompt-*.md`: временный `git worktree` от `origin/main`, коммит и пуш в `task/<slug>-<timestamp>`, затем перенос файла в `prompts/_dispatched/` (гитигнорится). Найден и исправлен баг PowerShell 5.1: при `$ErrorActionPreference='Stop'` любая строка stderr от `git` (прогресс, «From github.com...») кидает `NativeCommandError` даже при редиректе в `$null` — обёрнуто в `Invoke-Git` с временным `Continue`.
  - `.gitignore` — добавлены `prompts/_dispatched/` и `scripts/dispatch.log` (локальное состояние диспетчера, не версионируется).
  - `.claude/rules/github-automation.md`, `COWORK.md` — описан новый путь: файл в `prompts/` → автоматический пуш в `task/*` → воркер; kickoff-строка в чате остаётся как fallback на случай выключенной машины владельца.
  - Локально протестированы оба режима (`-Once` и watcher): фиктивный `prompt-ops-test-*.md` уходил в `task/*`-ветку за секунды после дебаунса; ветки и файлы после проверки удалены.
- **Дальше:**
  - Регистрация задачи в Task Scheduler (`ONLOGON`, автозапуск наблюдателя) — владелец решил пропустить в этом проходе; сделать вручную или отдельным промптом, когда понадобится автозапуск на этой машине.
  - Живой смоук-тест: реальный `prompts/prompt-*.md` от Cowork → проверить, что PR от воркера действительно открывается по `task/**`-пушу.
