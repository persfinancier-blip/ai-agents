# DEVLOG — часть 08

Записи с 2026-07-17 по 2026-07-22.

## 2026-07-22 · task/ops-11-cowork-direct-push-rules-20260722122918 · не закоммичено (промпт ops-11)

- **Что сделано:**
  - Канон приведён в соответствие с новой прямой записью Cowork в GitHub: Cowork теперь пушит `task/**`-ветки напрямую из своего sandbox по HTTPS, с fine-grained PAT в `.secrets/gh_token` (`Contents: Read and write`); SSH из sandbox недоступен.
  - `.claude/rules/commits.md` → секция «Push and responsibility» переписана: прямой пуш Cowork — основной путь диспетчеризации, watcher (`scripts/dispatch-tasks.ps1`) — fallback; добавлена сверка sync по хешам.
  - `.claude/rules/github-automation.md` → в «File-driven dispatch» добавлен буллет о прямом пуше как primary path, существующий буллет про installed copy помечен как «Fallback watcher».
  - `COWORK.md` → обновлены «Boundaries», «Push and responsibility (brief)», «Sandbox», «New prompt»: везде отражён прямой git-write Cowork, ограниченный веткой `task/**`.
- **Дальше:**
  - Множество старых записей DEVLOG с «не закоммичено» ещё не финализированы (см. `grep -rn "не закоммичено"` по `docs/devlog/`) — вне рамок этого прохода, оставлено на будущий `/devlog`.

## 2026-07-22 · task/prompt-ops-10-devlog-shard-20260722111638 · не закоммичено (промпт ops-10)

- **Что сделано:**
  - `docs/DEVLOG.md` (169 527 байт, 62 записи `^## `) разделён на `docs/devlog/part-01.md` … `part-08.md`: заполнение хронологическое от старейшей записи, каждая часть закрывается по достижении ~25 KB (не по фиксированному числу записей), внутри части — новые сверху, как раньше. Тела записей скопированы дословно (без правок).
  - `docs/DEVLOG.md` переписан в индекс: интро-абзац про новую структуру, таблица частей (файл/диапазон дат/число записей), список из 62 строк (по одной на запись, новые сверху) с хуком в 5–10 слов и ссылкой на часть.
  - `.claude/commands/devlog.md` переписан: Step 0 (финализация `не закоммичено`) теперь через `grep`, не полное чтение; определение текущей части — по наибольшему `part-NN.md` и его размеру (`wc -c`), ротация новой части при превышении 25 KB; абзац «перечитать файл и сверить diff мысленно» удалён — регрессия №39/№40 структурно невозможна на частях ≤25 KB.
  - Точечные правки указателей на новую структуру: `CLAUDE.md` («Token economy» → раздел про DEVLOG), `docs/PROCESS.md` (таблица §6 «Память и состояние»), `.claude/commands/ship.md` (шаг 3, DoD).
  - Верификация: сумма `grep -c "^## " docs/devlog/part-*.md` = 62 (совпадает с `grep -c "^## " docs/DEVLOG.md` на `main` до начала прохода — тоже 62, а не заявленные в промпте 61 — цифры промпта датированы 2026-07-18, с тех пор добавился слайс 46); `diff` отсортированных заголовков оригинала и конкатенации частей — пуст; суммарный размер частей 169 855 байт (оригинал 169 527 байт минус ~230 байт intro/heading, плюс 8 частей с заголовками part-NN); ни одна часть не превышает 25 KB (максимум — part-01.md, 25 370 байт, с учётом заголовка части).
- **Дальше:**
  - Эта запись — первый живой прогон переписанной команды `/devlog`: новая запись легла в `part-08.md` (текущая часть, 3 437 байт до вставки, остаётся ниже 25 KB и после неё) без создания `part-09.md`.
  - Объём чтения `/devlog` на следующий проход: хвост индекса (таблица частей + список, ≈13.4 KB) + текущая часть (`part-08.md`, после вставки ≈8 KB) — суммарно около 21 KB, комфортно ниже 30 KB. Как только `part-08.md` пересечёт 25 KB, следующий `/devlog` создаст `part-09.md`.

## 2026-07-17 · task/prompt-46-units-panel-tree-b · не закоммичено (промпт №46)

- **Что сделано:**
  - Слайс 46 — дерево департаментов + команды в панели юнитов (`UnitsPanel.tsx`), read-only, завершает юниты-UI milestone (список → группировка backend → пикер владельца → дерево). `UnitRead.department_id` добавлен в `types.ts` (backend уже отдавал поле); `listTeamMembers(teamId)` в `api.ts` (`GET /unit-groups/{id}/members`).
  - `UnitsPanel.tsx` переписан: единая загрузка `Promise.all([listUnits(), listUnitGroups()])`, затем состав каждой команды (`Promise.all` по `listTeamMembers`) в `teamId → unitId[]`. Секция «Департаменты» — лес по `unit_group.parent_id` (корень — `parent_id === null`), рекурсивный `DepartmentNode` с отступом `depth * 16px`; под каждым департаментом — его юниты по `department_id`; пустой департамент (без юнитов и детей) всё равно рендерится. «Без департамента» — плоский список юнитов с `department_id === null`/на несуществующий id. «Команды» — плоский список, состав резолвится в имена через `unitId → UnitRead`, пустая команда → «— пусто».
  - Стили: новые классы `.unit-section-caption`/`.unit-dept-row`/`.unit-dept-name`/`.unit-team-row`/`.unit-team-members` в `os.css`, без новых токенов/цветов (существующие `--ink`/`--i35`/`--ln`/`--mono`/`--sans`).
  - Backend не менялся (слайсы 41/44 уже отдавали всё нужное). `npm run build`/`npm run lint` — зелёные.
  - Playwright-проход (сабагент, headless Chromium через `npx playwright install chromium --with-deps`) на сидированной БД: корневой департамент «Инженерия» + дочерний «Бэкенд» (отступ +16px подтверждён), юнит в дочернем департаменте, юнит в корневом, юнит без департамента (секция «Без департамента»), команда «Команда роста» с 2 участниками (имена резолвятся, не «— пусто»). Скриншот — `renders/slice46-units-tree.png`. Багов в логике рендера не найдено.
- **Дальше:**
  - Юниты-UI milestone (список → группировка backend → пикер владельца → дерево) закрыт. Единственный опциональный хвост — UI редактирования членства (add/remove в команду, смена домашнего департамента юнита) — в бэклог, не запланирован.
  - Следующий слайс по роадмапу — п.2 (переходы процесса → `ProcessMap` на реальных данных) или п.4 (канвас навыка) по решению владельца.
